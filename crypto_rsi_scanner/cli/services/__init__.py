"""CLI service package for command-body extraction.

Service modules own implementation logic for command groups once extracted from
``scanner.py``. Existing command wrappers remain compatibility-preserving until
each body is moved with focused tests.
"""

from __future__ import annotations
