# ROADMAP

This file tracks pending work. `DEVLOG.md` records what changed; this file records
what still needs attention. Keep it short, current, and status-oriented.

Status labels:
- `todo`: ready to pick up.
- `waiting`: blocked on time, data, credentials, or a human decision.
- `doing`: actively in progress.
- `done`: completed; move details to `DEVLOG.md`.
- `dropped`: explicitly rejected; record the reason in `DECISIONS.md` if durable.

## Now

| status | item | owner | notes |
|---|---|---|---|
| doing | Deliver Decision Radar Empirical Validation and Calibration Lab v1 | Codex | Protocol, pure production Decision-v2 replay, immutable bundles, PIT fixture/Binance loaders, fixed episodes, 1/3/7/14d returns and BTC/ETH-relative MFE/MAE/timing/expiry outcomes, closed route/origin/score/regime/liquidity/data-quality/cost/burden cohorts, shadow/walk-forward/final-test firewall, and separate live projection are green. Full local inventory: 419 symbols, 468,622 bars, 8 explicitly excluded partial bars. Next: outcome-blind controls/missed baselines, immutable smoke/medium runs, final reports, and existing-dashboard Research Lab. |
| done | Deliver Decision Radar Operator Authority and Evidence Reality v1 | Codex | Validation, readiness, fixture rendering, integrated smokes, `verify-fast`, and full `verify` are authority-neutral; publication/invalidation are distinct guarded commands. One already-authorized, cadence-eligible CoinGecko no-send cycle published strict-clean real namespace `radar_market_no_send_20260716t020832349089z_ed303d3217c3` at revision 12, and its exact pointer digest survived every later gate unchanged. Evidence-cycle readiness now separates profile capability, current authorization, credentials, health/backoff, and call eligibility; because no genuine Event Alpha source is currently authorized it made zero calls/writes. Immutable digest-addressed source-independence storage, concise operator summaries, frozen family-indivisible OOS workflow, partial-calendar truth, and venue-neutral execution readiness are complete without threshold, route, send, trade, paper, RSI, fade, or authorization changes. |
| done | Improve North Star engine evidence quality and measurement | Codex | Eight evidence slices are shipped: impossible catalyst source clocks reject before attachment; catalyst authority uses exact canonical source identity; LLM frames bind one exact source; robust temporal surprise, fixed-start anomaly episodes, and frozen-representative Decision-v2 outcome scorecards remain descriptive; anomaly/catalyst attribution prevents post-hoc or context-only evidence from becoming causal confirmation; and the closed `event_alpha.source_independence` v1 contract now prevents syndicated or near-duplicate copies from inflating event, alert, incident, hypothesis, watchlist, Core, Decision, card, notification, or outcome corroboration. Raw updates, domains, content clusters, independent units, and additional corroborations stay separate; assessed/unassessed/rejected state and errors propagate explicitly; historical v1 projections remain readable without fabricated evidence. Exact candidate, CoreOpportunity, outcome, and campaign bindings remain copy-only and fail closed. The complete program made zero provider, send, trade, paper, RSI, fade, threshold, automatic-policy, publication, or authority changes. |
| done | Bound source-independence artifact amplification | Codex | New rows use one immutable digest-addressed, descriptor-anchored contract object plus closed compact references. Exact bytes/fingerprints/count summaries and the canonical store path are doctor/operator-bound; missing, mutated, symlinked, ambiguous, or foreign references fail closed. Legacy inline rows remain readable and retain their representation during atomic downstream rewrites. Current integrated-fixture measurement is 39,046 bytes / 4.032% smaller with one 8,990-byte store object, plus explicit read-performance and ZIP/export accounting. |
| done | Prepare leakage-safe source-independence OOS validation | Codex | Added immutable offline export/validate/report commands with required event-copy family IDs, deterministic family-indivisible development/review/test split v3, exact-source and normalized-content cross-split leakage rejection, closed label categories, pending/incomplete/complete states, and precision/recall/false-merge/missed-copy cohorts. No provider, route, score, normalization, 12-token minimum, or `0.80` threshold changed. |
| waiting | Label and validate source-independence threshold out of sample | human + Codex | Supply a source-diverse human-labeled corpus through the frozen OOS workflow. Pending labels intentionally return non-success; no policy conclusion is allowed until review/test coverage is complete. Any threshold or routing change still requires enough independent examples and explicit human approval. |
| waiting | Authorize a fresh Event Alpha evidence-validation source cycle | human + Codex | Current status: the profile is source-capable, but GDELT, RSS, Polymarket, CryptoPanic, official-exchange, and genuine local source authorization/configuration are absent, so the guarded cycle makes zero calls and exits before writes. Inspect with `make event-alpha-evidence-cycle-readiness PROFILE=notify_llm_quality PYTHON=python3`; the current safe action is its zero-call artifact preview. Do not create authorization automatically. |
| done | Deliver Crypto Decision Radar Daily Operations v1.1 | Codex | Split immutable attempt, final publication, and owned-dashboard operations evidence; bind the final receipts to exact namespace/run/revision/operator/artifact/doctor/pointer truth; reconcile only a uniquely proven prior success without providers or process work; and make campaign/readiness/doctor/dashboard authority agree. The owned dashboard can bootstrap only after the publication receipt and serves 503 until the operations receipt exists; its final probe requires exact authority headers and a stable positive owned PID. Pointer mutations are serialized through the originally verified root descriptor, rollback restores receipt-matching exact bytes only, and any later failed terminal row invalidates the same cycle's earlier success. Historical cycle authorization is separate from a 15-minute credential-free current-status receipt, GET/HEAD never inspect environment authorization, and eligible disabled maintenance gets 90-minute expiry guidance without automatic work. Official macro coverage can now be explicitly partial per source. Artifact-heavy tests isolate config and environment roots while preserving explicit call-site bases, report slow files, reuse process source/AST caches, block unreadable retention scans, expose bounded non-mutating retention pressure, and set a 360-second observational same-machine extracted-checkout `verify-fast` review budget distinct from the five-second focused guard. No service, provider authorization, venue, spread source, or phone route was enabled. |
| done | Deliver Crypto Decision Radar Daily Operations v1 | Codex | Added readiness/status/manual-cycle and confirmation-gated install/uninstall for one guarded local maintainer. Every cycle journals attempted/terminal state, rechecks authorization/cadence before at most one CoinGecko no-send request, re-reads post-attempt cadence without another call, uses a unique namespace, strict-doctors before publication, restarts only the exact owned dashboard, and rolls back or invalidates the failed new pointer on publication/restart/journal failure. Restart exceptions fail closed; a loaded scheduler with a non-zero last exit is unhealthy. System Health and Run History expose bounded maintenance truth without launchd/provider/write work on GET/HEAD. The service remains prepared and uninstalled. |
| done | Add authoritative Fed/BLS/BEA macro-calendar production | Codex | Added pure Fed FOMC, BLS CPI/employment, and BEA PCE/GDP parsers plus independent guarded acquisition and explicit local import. Live is off without authorization; BLS is `missing_configuration` without an honest contact; configured calls are one each with no redirect/retry. Each source records observed/no-results/unavailable/missing-configuration/parse-error/rate-limited truth, while complete/partial/unavailable snapshots preserve immutable accepted bytes and exact coverage. Local imports accept genuine source subsets with real acquisition time and reject checked-in fixture/test/mock/replay paths. Latest complete or partial success is hash-attested across pointer, receipt, snapshot, coverage, and accepted raw sources before Daily Operations may attach it. Current live authority honestly remains `not_configured` because no calendar call or genuine operator export occurred. |
| done | Prepare venue-neutral execution-quality integration | Codex | Added a static/no-network contract covering feasible centralized spot, perpetual, and operator-selected DEX modes, public/auth boundaries, jurisdiction/network constraints, request limits, and spread/depth/impact fields. No venue is selected and no live adapter, order, or execution behavior exists. |
| waiting | Identify the intended execution venue and instrument mode | human | Current status: no venue or spread provider selected, so spread-unavailable ideas cannot qualify actionable/rapid. Implication: choose the actual spot, perpetual, or DEX surface and confirm jurisdiction/network constraints before adapter design; this is not authorization for orders or execution. Safe no-network command: `make radar-execution-quality-readiness PYTHON=.venv/bin/python`. Expected provider activity: none. Rollback: none required because no adapter is active. |
| done | Add explicit temporary public phone access | Codex | Added a confirmation-gated Cloudflare Quick Tunnel that keeps the backend loopback-only, requires the exact local dashboard identity, pins HTTP/2 and non-debug fixed argv, waits for edge registration, and publishes only after the random `trycloudflare.com` URL returns the expected HTTP 200 dashboard. Confirmed disable stops only the exact owned live command. Live phone URL was verified without an account, domain/DNS setup, credentials, startup service, Funnel, LAN bind, router port, provider call, send, trade, or artifact mutation. |
| done | Keep expected dashboard freshness expiry useful and fail-closed | Codex | A pointer-bound generation whose exact identity still matches but is stale now returns the full quarantined diagnostic shell under HTTP 503: System Health, historical Outcomes, and labeled Run History remain inspectable, all current rows/fields/counts stay suppressed, and phone readiness remains blocked. Identity, pointer, fingerprint, schema, and integrity failures retain the minimal unavailable response. The six-hour age guard is unchanged. |
| waiting | Decide whether to install the prepared Daily Operations service | human | Current status: prepared/disabled. Implication: freshness requires manual action; readiness itself makes no provider call, while an installed eligible cycle may attempt at most one already-authorized CoinGecko no-send request plus an owned restart after strict-clean publication. Inspect with `make radar-daily-ops-readiness PYTHON=.venv/bin/python`; install only with `CONFIRM=1 make radar-daily-ops-install PYTHON=.venv/bin/python`; roll back with `CONFIRM=1 make radar-daily-ops-uninstall PYTHON=.venv/bin/python`. Installation never creates provider authorization. |
| waiting | Decide whether to enable phone dashboard access | human | Current status: private Tailscale Serve and public Cloudflare Quick Tunnel are disabled; the backend remains loopback-only. Private readiness: `make radar-dashboard-phone-readiness PYTHON=.venv/bin/python`; public readiness: `make radar-dashboard-public-readiness PYTHON=.venv/bin/python`. Neither calls market providers or enables access. Enable only with the matching exact `CONFIRM=1 ...-enable` command and roll back with its confirmed disable command. The public route is temporary and unauthenticated; private Tailscale remains the persistent recommendation. |
| done | Add private authenticated phone access to the Decision Radar dashboard | Codex | Kept the HTTP backend loopback-only and added a fail-closed Tailscale Serve operator workflow for private tailnet HTTPS. Readiness/status are observational; enable/disable are confirmation-gated; stale dashboard authority, offline Tailscale, missing tailnet DNS, configured Funnel, or a conflicting HTTPS handler blocks exposure. The helper never adds a dashboard secret, LAN/wildcard bind, router rule, public tunnel, Funnel route, or broad Serve reset. Dashboard responses now also deny framing and suppress referrer and sensitive browser capabilities. |
| done | Complete the professional Decision Radar UX and truth-language pass | Codex | Re-rendered every primary workflow across 320, 390, 768, 1024, 1200, 1366, and 1440px and rebuilt the hierarchy around current operator attention. Today now uses route-aware language and compact review cards; active calendar risk leads scheduled rows; Market links scanner evidence to its canonical Decision, prioritizes measured moves, uses dense wide tables and balanced laptop/mobile cards, and keeps provider slugs secondary; Outcomes and Run History use compact current-run identity and explicit unavailable states; Health leads with integrity, coverage, and linked action rows. Supporting filters, matrices, technical lineage, historical outcomes, expired rows, and past events remain collapsed until requested. Relative time uses one exact-generation clock, missing booleans stay tri-state, date-only events remain current through their known day, and expired diagnostics still require explicit opt-in. Exact-generation, read-only, no-send/no-trade, canonical Decision v2, and fail-closed coverage contracts are unchanged. |
| done | Deliver Crypto Decision Radar Operator Experience V1 | Codex | Replaced the report-like dashboard with one responsive seven-page decision terminal: Today, Market Radar, Ideas and detail, Calendar, System Health, Outcomes & Learning, and Run History. One closed presentation layer formats time, field-level units, enums, scores, empty states, and technical disclosures without re-evaluating Decision v2. Exact current rows stay separate from bounded historical context; the market funnel distinguishes provider, selection, observation, anomaly, integrated-candidate, and Core/operator counts without inventing a causal market chain for mixed generations. One seven-layer coverage model includes market, catalyst, calendar, derivatives, RSI, outcomes, and request ledger. Untrusted current data is centrally quarantined, fingerprint-failed current artifacts cannot be demoted into history, credential-bearing URLs are rejected, and turnover ratios render as percentages. Pointer-bound GET/HEAD requests revalidate authority, the read model avoids cumulative source-cache scans, and browser/UX smokes cover desktop through mobile without provider calls or writes. |
| done | Make sparse Decision Radar generations truthful across every dashboard layer | Codex | A zero-idea authority now exposes its exact evaluated market observations/anomalies, source-pack coverage, provider state, calendar ingestion/normalization status, and separately labeled cumulative campaign outcomes. Healthy-empty, unconfigured, degraded, stale, and rejected are distinct. Dashboard reads are descriptor-anchored to one namespace and reconcile semantic counts; raw anomaly evidence stays separate from Decision candidates. Same-cycle anomaly zeroes require a reconciled completion receipt. Market quality uses the full snapshot universe; optional calendar snapshots are safely copied/fingerprinted only from an explicit non-fixture local path, and scheduled/unlock/unified derivatives are recomputed from that copy before publication. No provider authorization or call was added. |
| waiting | Authorize official macro acquisition or supply a genuine source export | human + Codex | Current status: calendar authorization absent and no genuine latest-success snapshot, so the current generation remains `not_configured`; no call is permitted. Inspect with `make radar-calendar-official-readiness PYTHON=.venv/bin/python` (no call/write). An authorized acquire may call each configured source once; BLS remains `missing_configuration` without an honest contact. Alternatively import any genuine operator-downloaded Fed/BLS/BEA subset with its real acquisition time. Do not use checked-in fixtures; they are rejected. Rollback is unnecessary because the application never creates authorization and an unavailable attempt cannot replace prior success. |
| done | Prevent a stalled local browser connection from freezing the dashboard | Codex | Replaced the single-client WSGI serving path with a loopback-only threaded server while preserving GET/HEAD-only, read-only, exact-authority behavior. A deterministic real-socket regression parks one incomplete request and proves a second GET succeeds without artifact mutation. Refreshed the strict-clean fixture authority and launched the dashboard as a transient user launchd job; it remains visibly fixture/no-send/campaign-excluded. |
| done | Harden Decision Radar Observation Campaign v2 evidence | Codex | Separated Decision market measurements from Event Alpha catalyst burn-in; enforced a default one-hour minimum cadence with too-close observations retained but excluded from baselines; added feature/time-aware warmup groups, stable unchanged-doctor authority digests, canonical market-context lineage, sanitized HTTP telemetry, and a shared pending/mature outcome ledger. `make radar-market-campaign-report` now rebuilds the local artifact-derived campaign truth and next safe command without providers. Authorization and cadence are rechecked before the adapter, and one eligible invocation may attempt at most one bounded live request. |
| done | Productize Decision Radar Live Pilot v1 evidence and publication contracts | Codex | Added canonical market provenance v2 with derived live/mock Decision-campaign truth plus legacy compatibility aliases; distinct fingerprinted request/source lineage; bounded per-asset temporal history with explicit warmup and direct/proxy feature bases; missing-spread, proxy-only, and cold/warming quality caps; one pending outcome per canonical idea; credential-free pilot audits; and real-only strict-clean dashboard publication. Mock/fixture runs prove mechanics but never count in the real Decision campaign or become real authority. Authorization-absent runs attempt no call and leave the pointer unchanged. All paths remain research-only/no-send/no-trade/no-paper/no-RSI/no-trigger. |
| done | Close Decision Model v2 projection consistency and guarded real/no-send market authority | Codex | Froze `crypto_radar_decision_projection_v1` as the copy-only candidate/Core/card/preview/outcome/dashboard truth; preserved calendar and RSI context; proved all eight routes, return-unit/freshness/spread/expiry gates, Decision-first cards, strict cross-artifact drift checks, and descriptor-anchored market artifact writes. Added `radar-market-no-send-*` with explicit CoinGecko authorization and real-only pointer publication. Event Alpha remains the additive Catalyst Radar; no sends, trades, paper trades, normal RSI writes, execution, or Event Alpha-created `TRIGGERED_FADE`. Product contracts are split between `CRYPTO_DECISION_RADAR_NORTH_STAR.*` and the retained Catalyst Radar contract. |
| waiting | Continue the trusted Decision Radar observation campaign at the enforced cadence | human + Codex | Eight authorized/countable CoinGecko cycles now exist. The current exact live authority is zero-idea namespace `radar_market_no_send_20260716t020832349089z_ed303d3217c3` (run `2026-07-16T02:08:32.428755+00:00\|no_key_live`, revision 12), strict-clean with dashboard HTTP 200. Campaign history has 240 real retained observations across 33 assets: 210 baseline-counting, 30 explicitly too-close, and 0 globally warm. Volume/turnover remain warming; return, volatility, and BTC/ETH-relative groups remain cold or warming. Historical evidence contains 2 `risk_watch` ideas with 1 matured outcome and 1 outcome due but missing price data; current ideas are 0 and spread coverage remains unavailable. Start with `make radar-market-no-send-readiness PYTHON=.venv/bin/python`; never run rapid cycles or lower thresholds. |
| done | Productize the Crypto Radar Decision Model v2 vertical | Codex | Hardened the source+artifact export against directory/final-file replacement races; added market-led TESTFLOW plus blocked TESTFLOWLOW coverage; preserved ordered thesis origins; added dashboard/actionable/rapid/high-confidence/fade/risk/calendar routes, explicit spread policy, timing/urgency/expiry/chase risk, coherent return units, v2-first cards/brief/inbox/preview/dashboard fields, complete fingerprinted pointer readiness with post-start drift binding, Calendar v2 overlays, exact read-only RSI context fusion, and one pending outcome per explicit v2 idea. Fresh fixture run: 13 candidates, 10 Core rows/cards, 13 pending outcomes, zero doctor blockers, authoritative dashboard pointer, no sends/trades/paper/RSI writes/triggers. Next work is real no-send evidence collection, not more routing refactor. |
| done | Harden the Crypto Radar v2 evidence loop overnight | Codex | Eleven integrity slices are complete: v2 single-source safety, exact fingerprint/fresh-doctor dashboard authority, payload-free calendar loss accounting, fail-closed outcome and feedback firewalls, a timestamp-aligned observed-price builder, resolved-context isolation, a preview-first exact observed-outcome operator with confirm-gated create-only noncanonical staging, current-window measurement without stale historical narratives, and one closed evidence clock plus exact conflict-safe Core cohort authority, typed feedback progress, producer-shaped provider evidence, immutable historical outputs, and all five North Star readiness thresholds across measurement/scorecard/source-yield. Runtime priors remain unconditionally shadow-only. Next collect real versioned observed close sets, add longitudinal exact-evidence cohorts only when samples exist, and keep auditing operator/provider paths. Keep every path research-only/no-send/no-trade/no-paper/no-RSI/no-trigger. |
| done | Rotate the exposed Telegram bot token | human + Codex | The owner revoked/replaced the token through BotFather, updated the ignored local `.env`, and Codex restarted `com.nasrenkaraf.rsibot`; `make status` confirmed the listener healthy without exposing the replacement credential. |
| waiting | Configure and rehearse Coinalyze first | human + Codex | North Star activation step 1. Configure `RSI_EVENT_DISCOVERY_COINALYZE_API_KEY` locally, then run the bounded request-ledger-backed no-send preflight/rehearsal. Do not enable sends. |
| waiting | Restore external provider access/capacity | human | The current CryptoPanic token belongs to the discontinued Developer plan while the supported Growth route requires a matching active token; OpenAI returns `insufficient_quota`; and Bybit CloudFront blocks the current egress country. Replace/upgrade the CryptoPanic token and restore OpenAI billing/quota. Choose a permitted Bybit network route only if it complies with the account/jurisdiction; the code will not bypass the restriction. |
| waiting | Rehearse official exchange announcements second | human + Codex | The bounded Bybit public no-send rehearsal is complete and now reports the current 403 accurately as `region_restricted` after one request, with zero sends/trades/paper/RSI writes/triggers. Actual Bybit evidence remains blocked on a permitted egress decision. Binance public/fixture validation remains next after the North Star Coinalyze-first review; the signed Binance listener still needs explicit credentials and review. |
| waiting | Monitor GDELT migration and evaluate Web NGrams | system + Codex | GDELT still returns upstream HTTP 429 during its documented migration. Automatic profiles now retain one broad context fetch, remove the redundant catalyst-search call, and enter provider backoff on the first 429. Re-evaluate a bounded Web NGrams context adapter after the upstream format/service stabilizes; do not restore duplicate DOC API calls. |
| done | Ship Crypto Radar Decision Model v2 and local operator dashboard | Codex | Added explicit market/catalyst/technical/macro thesis dimensions, independent actionability/evidence/risk scoring, fail-closed market-led gates, separate research routes, transparent cards/previews/outcome/feedback/operator propagation, a unified fixture calendar, and an exact-revision GET/HEAD-only local dashboard. Legacy opportunity/notification routes remain unchanged; old rows are not promoted; all paths remain no-send/no-trade/no-paper/no-RSI/no-trigger. |
| done | Stabilize failing RSS/API/LLM provider paths | Codex | Honest RSS headers plus one transient retry restored all four direct publisher feeds (110 recent rows, zero warnings). CryptoPanic uses the current `/api/growth/v2` route and distinguishes token/plan mismatch. Bybit records bounded redacted diagnostics and classifies auth/rate/region/edge failures. GDELT no longer receives duplicate automatic calls, and OpenAI shares a run circuit breaker with three-call parallelism plus explicit success/failure/backoff telemetry. All paths remain research-only and no-send by default. |
| done | Close operator-semantic contradictions and guarded provider candidate path | Codex | Exact CryptoPanic configuration, source blockers, canonical counters/freshness/no-send scopes, partial-provider readiness, unique attempt lineage, fail-closed contract counting, bounded Bybit/Coinalyze paths, and max-20 targeted market refresh are complete. Active exact run remains 108 raw / 48 candidate / 48 research / 185 core / 105 visible / 0 strict; strict doctor has zero blockers, no provider flags were enabled, and real contract evidence remains zero. |
| done | Close Pro-review verification, coherence, retention, and identity gaps | Codex | Exact-generation operator state, strict-doctor revision authority, canonical custom-ledger locking, shared writer/retention exclusion, expanded fail-closed retention, descriptor-safe archive hermeticity, single-character identity pairing, and all seven provider ranks are complete. Final no-send run: 108 raw / 48 candidates / 7 validated / 185 cards / 0 alertable / 0 attempted; strict doctor 1,056 rows, zero blockers; full local gate 835 standalone + 910 pytest. |
| done | Clear active Event Alpha artifact-doctor blockers | Codex | Reconciled guarded historical send facts, atomically migrated legacy absolute operator paths, fixed market return-unit inference, scoped burn-in measurement explicitly, normalized precise pre-incident watchlist rows, and stabilized first-write Core Opportunity/card grouping. Full local verification passes 791 standalone plus 866 pytest tests; strict `notify_no_key` doctor validates 491 rows with zero schema/path/safety errors and no blockers. Remaining WARN rows are research-quality telemetry, not readiness defects. |
| done | Refresh local verification runtime evidence | Codex | Replaced the stale July 2 timing artifact with a current no-live measurement: standalone 786/786 takes 191.610s and pytest 861/861 takes 198.114s. This confirms targeted gates for ordinary prompts, `verify-fast` for broader non-release work, and full duplicate verification only at release/risky/cluster boundaries. |
| done | Split notification runtime helpers from CLI config reports | Codex | Moved the budget class plus six budget/warning/next-step helpers into a 126-line module, preserving exact ASTs and `__all__` exports. Kept the config-coupled empty-pipeline constructor in place after the broad test exposed its dependency. `config_reports.py` fell from 1,392 to 1,295 lines, and all 67 notification/CLI tests pass. |
| done | Split opportunity-verdict value and evidence helpers | Codex | Moved 8 generic value/score helpers and 7 narrative/official/market evidence predicates into 132- and 264-line modules, preserving exact ASTs and private re-exports. `opportunity_verdict.py` fell from 1,395 to 1,056 lines, and all 144 verdict consumer tests pass. |
| done | Split opportunity-audit normalization and matching helpers | Codex | Moved 15 normalization/value helpers and 4 card/feedback/target matching helpers into 182- and 130-line modules, preserving exact ASTs and private re-exports. `opportunity_audit.py` fell from 1,404 to 1,145 lines, and all 98 audit/core/operator/router consumer tests pass. |
| done | Split pure shim-report formatting from shim audits | Codex | Moved five Markdown formatters and the legacy compatibility-test path into `shim_formatting.py`, preserving every `shims.py` export and byte-exact output. The near-threshold audit module fell from 1,431 to 1,169 lines, the formatter is 286 lines, and all 23 shim/old-import tests pass. |
| done | Split static architecture-report contract data | Codex | Moved schema/output names, major-target metadata, tracked paths, split-path inventory, and migrated-module history into `architecture_report_contract.py` while re-exporting all 11 names unchanged. The near-blocker generator fell from 1,472 to 1,398 lines, the contract is 93 lines, and 18 architecture/project-health tests pass. |
| done | Remove obsolete Event Alpha helpers from the standalone umbrella | Codex | Proved that 747 lines of Event Alpha fixtures formed a dead duplicated helper graph after the package splits, removed them and their unused imports, and reduced `tests/test_indicators.py` from 1,698 to 913 lines. All 41 local identities remain exact, focused pytest passes, and the complete standalone runner passes 786/786. |
| done | Retire the namespace-lifecycle test monolith | Codex | Partitioned all 30 profile/clock/notification, ledger/lock, scheduled-catalyst/unlock, and cross-namespace lifecycle tests into four focused modules ranging from 242 to 683 lines, then deleted `test_namespace_lifecycle.py`. Every identity remains exact/unique, all 30 pass under pytest and standalone, and the full Event Alpha package passes 650 tests. |
| done | Retire the source-coverage test monolith | Codex | Partitioned all 35 coverage-report, catalyst-search, source-registry/pack, and evidence-acquisition tests into four focused modules ranging from 405 to 1,214 lines, then deleted `test_source_coverage.py`. Every identity remains exact/unique, all 35 pass under pytest and standalone, and the full Event Alpha package passes 650 tests. |
| done | Retire the artifact-doctor test monolith | Codex | Partitioned all 47 public/schema/provider-readiness, notification, quality, reconciliation, provider-conflict, and integrated-safety tests into five focused modules ranging from 623 to 1,082 lines, then deleted `test_artifact_doctor.py`. Every identity remains exact/unique, all 47 pass under pytest and standalone, and the full Event Alpha package passes 650 tests. |
| done | Retire the outcomes test monolith | Codex | Partitioned all 49 evidence-quality, alert-outcome, feedback/calibration, burn-in/replay, and quality-feedback tests into five focused modules ranging from 588 to 1,023 lines, then deleted `test_outcomes.py`. Every identity remains exact/unique, all 49 pass under pytest and standalone, and the full Event Alpha package passes 650 tests. |
| done | Retire the notification test monolith | Codex | Partitioned all 66 planning, routing, readiness, delivery, lane, operations, inbox, and provider-rehearsal tests into seven focused modules ranging from 393 to 1,019 lines, then deleted `test_notifications.py`. Every identity remains exact/unique, all 66 pass under pytest and standalone, and the full Event Alpha package passes 650 tests. |
| done | Retire the provider-readiness test monolith | Codex | Partitioned the final 48 activation/readiness/health, discovery-pipeline, and cache/scanner-report tests into `test_provider_activation.py` (1,356 lines), `test_discovery_pipeline.py` (940), and `test_discovery_cache_reports.py` (817), then deleted `test_provider_readiness.py`. All original 88 identities remain exact/unique, all residual tests pass pytest/standalone, and the full Event Alpha package passes 650 tests. |
| done | Split exchange/universe and market-data provider tests | Codex | Moved 16 official exchange, manual/CoinGecko universe, Binance/Bybit, calendar, normalization/dedupe, resolution, and direct-no-trade tests into `test_exchange_universe_providers.py` (699 lines), plus 10 Coinalyze/DEX/supply tests into `test_market_data_providers.py` (534). Provider readiness fell from 4,277 to 3,079 lines; all 74 names remain exact/unique and pass together, and all 26 moved tests pass standalone. |
| done | Split provider-readiness news tests | Codex | Moved 14 CryptoPanic, GDELT, project-RSS, external-asset inference, event-time provenance, classifier-confidence, and no-trade safety tests into `test_news_providers.py` (1,113 lines). `test_provider_readiness.py` fell from 5,379 to 4,277 lines; all 88 source-surface names remain exact and unique, all 88 pass together, and all 14 moved tests pass through the standalone adapter. |
| done | Retire the integrated-radar test monolith | Codex | Partitioned the final 31 tests into `test_watchlist_router.py` (1,156 lines), `test_operator_workflows.py` (1,006), and `test_operator_presentation.py` (618), then deleted `test_integrated_radar.py`. All 31 residual names and all original 240 integrated-radar identities remain exact/unique, all 31 pass under pytest and standalone, the full Event Alpha package passes 650 tests, and the full standalone runner passes 786/786. |
| done | Split integrated-radar pipeline/scanner tests | Codex | Moved 12 radar report, operating pipeline, search validation, hypothesis-store, rejected-brief, LLM suggestion/exclusion/skip, extraction-order, scanner-cycle, and upstream-hint tests into `test_radar_pipeline.py` (1,088 lines). The source monolith fell from 3,832 to 2,756 lines; all 43 source-surface names remain exact and unique, all 43 pass together, and all 12 moved tests pass through the standalone adapter. |
| done | Split integrated-radar impact-hypothesis tests | Codex | Moved 9 generation, contextual matching, category refinement, identity/path validation, persistence, verdict, watchlist identity, and external-entity exclusion tests into `test_impact_hypotheses.py` (920 lines). The source monolith fell from 4,740 to 3,832 lines; all 52 source-surface names remain exact and unique, all 52 pass together, and all 9 moved tests pass through the standalone adapter. |
| done | Split integrated-radar market and playbook/graph tests | Codex | Moved 5 CoinGecko enrichment, anomaly, search-loop, and lifecycle tests into `test_market_enrichment.py` (387 lines) and 3 deterministic playbook/graph tests into `test_playbooks_graph.py` (531 lines). The source monolith fell from 5,634 to 4,740 lines; all 60 source-surface names remain exact and unique, all 60 pass together, and all 8 moved tests pass through the standalone adapter. |
| done | Split integrated-radar fade/resolver and alert-ranking tests | Codex | Moved 15 pure event-fade/resolver tests into `test_fade_core.py` (435 lines) and 6 alert ranking/tier/trigger-guard tests into `test_event_alert_ranking.py` (377 lines). The source monolith fell from 6,421 to 5,634 lines; all 81 source-surface names remain exact and unique, all 81 pass together, and all 21 moved tests pass through the standalone adapter. |
| done | Split integrated-radar catalyst-frame tests | Codex | Moved 13 catalyst classification, strategic-frame, LLM validation/runtime, operating-cycle, e2e artifact, missing-provider, asset-role, aggregation, and unresolved-frame cap tests into `test_catalyst_frames.py` (1,127 lines). The source monolith fell from 7,536 to 6,421 lines; all 94 source-surface names remain exact and unique, all 94 pass together, and all 13 moved tests pass through the standalone adapter. |
| done | Split integrated-radar incident semantics tests | Codex | Moved the final four claim-semantics, incident-relevance, upgrade-path, audit, daily-brief, and research-card tests into `test_claim_semantics.py` (867 lines) and `test_incident_relevance.py` (639 lines). The source monolith fell from 9,018 to 7,536 lines; all 98 source-surface names remain exact and unique, all 98 pass together, and all four moved tests pass through the standalone adapter. |
| done | Split integrated-radar LLM tests | Codex | Moved the contiguous 27-test relationship analyzer, advisory, OpenAI timeout, cache/budget, golden-eval, raw extractor, and scanner-report surface into `test_llm_radar.py` (942 lines). The source monolith fell from 9,953 to 9,018 lines; all 125 source-surface names remain exact and unique, all 125 pass together, all 27 pass through the standalone adapter, and standalone totals remain 786. |
| done | Split integrated-radar merge policy from candidate assembly | Codex | Moved 59 pure normalization, selection, scoring, market, derivatives, source, and summary helpers into `merge_policy.py` while retaining wrapper-synced compatibility exports. The near-blocker merge module fell from 1,498 to 551 lines, the policy module is 1,016 lines, six fixed semantic candidate hashes prove identity/source/market/derivatives behavior, and the full Event Alpha package passed all 650 tests. |
| done | Split daily burn-in command planning from execution | Codex | Extracted the immutable step model and pure smoke/candidate/daily command construction into `daily_burn_in_plan.py` while preserving the public imports and exact plan/render hashes. The stateful orchestrator fell from 1,499 to 1,159 lines, its obsolete over-1,200 architecture exception was retired, all 53 burn-in tests passed, and both no-send fixture smokes plus scoped/strict doctors passed. |
| done | Split event-fade validation and review workflow tests | Codex | Moved 45 validation-contract, human-label, sidecar, bundle, cache, and scanner workflow tests into `test_fade_validation.py` (1,081 lines) and `test_fade_review_workflows.py` (1,309 lines). The integrated-radar monolith fell from 12,311 to 9,953 lines; the complete 240-name surface remains exact, targeted pytest and standalone-adapter runs passed all 45 moved tests, and the Event Alpha package passed all 647 cases. |
| done | Split the largest integrated-radar test surface | Codex | Moved the final 70 core-opportunity, reconciliation, identity, and market-surface tests from `tests/event_alpha/test_integrated_radar.py` into four focused modules below 1,500 lines. The monolith fell from 16,234 to 12,311 lines with all 240 test names preserved exactly, standalone totals unchanged, `240 passed`, and architecture size/final gates still passing with `new_violation_count=0` and no additional over-1,500 test file. Further cohesive groups remain to split. |
| done | Audit and eliminate backup/runtime debris | Codex | The live backup set had the intended 14 valid snapshots plus 34 zero-byte WAL / SHM pairs (68 debris files), including 20 pairs orphaned by prior retention. All 14 backups passed immutable integrity checks and the newest passed a four-table restore drill before sidecar removal; five `.DS_Store` files were also removed while DBs, logs, caches, research artifacts, and retained snapshots stayed untouched. Verification now uses immutable read-only SQLite URIs, rejects non-empty backup WALs, pruning removes paired sidecars, and status exposes sidecar/temp debris. |
| done | Pin GitHub Actions to supported immutable releases | Codex | Replaced every mutable Node 20-era `checkout@v4` / `setup-python@v5` reference with the exact release commits for Node 24-based `checkout` v7.0.0 and `setup-python` v6.3.0. A repository-wide workflow regression requires 40-character SHAs plus readable release comments, while weekly Dependabot remains responsible for proposing reviewed updates. |
| done | Reproduce dependencies and enforce Python CI parity | Codex | Direct intent now lives in `requirements.in`; generated `requirements.txt` pins 28 universal Python 3.11+ packages with 915 SHA-256 hashes and compatibility markers. Bootstrap/CI enforce hashes, uv proves lock freshness, pip-audit runs on 3.11 and 3.13, full `make verify` coverage is matrixed across both, manual Event Alpha smokes use the same matrix, and Dependabot tracks pip/actions weekly. Clean hash installs passed the full local gate on 3.11.15 and 3.13.5. |
| done | Make verification artifact-hermetic | Codex | Daily burn-in now validates the checked-in North Star contract through a read-only command instead of regenerating tracked JSON/Markdown on every smoke/test/operation. A fake-repo sentinel regression proves real subprocess execution leaves the authored contract byte-identical, invalid contracts fail validation, explicit authoring targets remain writable, and `verify-fast` leaves tracked status unchanged. |
| done | Audit extreme paper-trade outcomes | Codex | Reviewed all seven closed rows with absolute return at least 50%. Stored entry/exit prices recompute exactly, nearest crossing signals match identity/entry price, independent 7-day outcomes agree directionally, and CoinGecko price/market-cap histories support genuine extreme moves rather than a database identity or redenomination defect. The scoreboard and paper-risk report now show trimmed means and retained diagnostic outlier rows without changing trades, scoring, routing, or thresholds. |
| done | Unify Event Alpha operator readiness gates | Codex | Fixed the checklist, v1-readiness, and burn-in-pack runtime exceptions; made the policy-scoped 30-day North Star scorecard authoritative for promotion/calibrated-send maturity; separated feedback collection, no-send operational readiness, and contract maturity; and kept every lane frozen at the current 4/20 cycles, 0/300 candidates, 0/150 labels, 0/50 near misses, and 0/100 outcomes. |
| done | Restore green Linux GitHub Actions verification | Codex | Ubuntu exposed a persisted `namespace_dir_abs_debug=/tmp/...` field that the macOS-only `/tmp/` assertion missed. Review inbox JSON now strips all `*_abs_debug` fields, the regression checks semantic paths plus the actual pytest temporary root, and CI runs one canonical `make verify` instead of repeating standalone/pytest under the previous 20-minute timeout. |
| done | Contain credential exposure and harden runtime privacy | Codex | Scrubbed the ignored log without printing the token, removed the real recipient ID from tracked examples, enforced `0600` runtime files/`0700` backup directories plus launchd `077` umasks, expanded configured credential/recipient redaction, and made the Pro-review ZIP exact-value scan fail closed before replacement. |
| done | Adopt risk-based verification cadence | Codex | Full pytest/`make verify` is no longer the default for every small prompt. Ordinary changes should use targeted pytest/compileall/relevant smoke or static checks, while full `make verify` remains the release/risky/shared-code/provider-activation/periodic 5-10 prompt sweep gate. |
| done | Run safe candidate-mode burn-in gate | Codex | `live_burn_in_20260709` completed as `passed_no_candidates` with `live_provider_calls_allowed=false`, real/contract candidates at 0, Coinalyze skipped for missing usable config, Bybit skipped for absent explicit allow flag, latest namespace strict doctor OK, archive dry-run `secret_hit_count=0`, and `research/EVENT_ALPHA_CANDIDATE_MODE_RUN_REPORT.md/json` records next safe commands. |
| done | Fix candidate-mode review inbox path portability | Codex | Candidate-mode fixture cards and daily review inbox items now use artifact-aware relative paths in both repo and temp artifact bases, stale path validation resolves against repo/artifact/namespace roots, and scoped/full doctor checks block absolute inbox paths, Markdown path leaks, missing selected card reasons, and inbox self-blockers. |
| done | Prepare provider-backed burn-in candidate path | Codex | Candidate-mode smoke selected items now have cards or explicit no-card reasons, `TESTFADE`/`TESTPERP` cards show lane/evidence/research-only safety copy, daily readiness distinguishes missing config vs config-ready/no-live vs ready, daily run artifacts include live provider guardrails, and source-yield avoids treating missing/blocked providers as failed yield. |
| done | Harden candidate-mode burn-in artifact hygiene | Codex | Archive dry-run now distinguishes safe missing/redacted credential status text from real secret-like values, persisted burn-in stdout/stderr tails are scrubbed with redaction counters, candidate-mode fixture smoke writes support artifacts/cards/review inbox paths, and strict smoke/live namespace doctors are clean without live calls or sends. |
| done | Harden candidate-mode daily burn-in operations | Codex | Candidate-mode daily no-send burn-in now uses a scoped bounded doctor by default, writes partial/final run status with explicit no-candidate/skip/timeout/interrupted states, persists full per-step metadata including skipped-step commands, has a readiness report and deterministic fixture candidate smoke, and preserves no-live/no-send/no-trade safety counters. |
| done | Add explicit daily burn-in candidate mode | Codex | `--event-alpha-burn-in-candidate-mode` / `CANDIDATE_MODE=1` now writes `event_alpha_candidate_mode_manifest.json`, keeps provider live calls disabled unless configured plus explicitly allowed, separates contract-counted candidates from support diagnostics, adds source-yield activation status, and passes strict doctor for `live_burn_in_20260707` with no warnings. |
| done | Polish daily burn-in traceability and skip statuses | Codex | `live_burn_in_20260705` strict doctor now exits OK/no warnings with `schema_validation_errors=0`, `source_coverage_provider_status_unknown=0`, and `integrated_preview_lane_mismatch=0`. Daily burn-in run steps persist required/start/finish/duration/timeout metadata, skipped Coinalyze rehearsal records an explicit no-live allow-flag reason, integrated delivery rows link the dedicated integrated preview, and review inbox fallback reasons are more specific without changing send/trade/provider behavior. |
| done | Make burn-in evidence semantics exact | Codex | Burn-in scorecard, measurement, source-yield, archive, review inbox, and doctor checks now distinguish burn-in run presence from real candidate evidence. Fixture/preflight/readiness/source-coverage rows no longer count toward contract candidate evidence or source-yield candidate yield, review inbox rows carry provenance and burn-in eligibility, and legacy source-yield artifacts remain doctor-compatible. |
| done | Tighten burn-in namespace scope and review inbox grouping | Codex | Default burn-in scorecard/measurement/source-yield/archive now count only active burn-in evidence and expose explicit include flags for notification rehearsals, no-key diagnostics, provider rehearsals, fixtures, and stale namespaces. Explicit single-namespace runs remain diagnostic unless counted intentionally, the daily plan target is no-write/no-live by default, and review inbox rows collapse duplicate visible families while ranking higher-value evidence gaps first. |
| done | Harden daily burn-in validation loop | Codex | Daily no-send burn-in now prints progress, enforces per-step timeouts, writes partial run artifacts after every step, has a quick fixture-only smoke target, and uses a namespace policy for scorecard/measurement/source-yield/archive scope. Review inbox ranking now records review value/diversity metadata, and archive supports dry-run with checksum and secret-scan summaries. |
| done | Fix burn-in validation-loop command wiring | Codex | Bare `--event-alpha-burn-in-review` now writes the artifact-only daily review inbox and no longer falls through to the default scan. Validation-loop project-health checks now catch missing Make target modules, missing no-send guards, unsafe send references, and review dispatch regressions. |
| done | Operationalize Event Alpha burn-in loop | Codex | Added the separate burn-in contract, daily live no-send burn-in runner, review inbox, feedback progress, scorecard, weekly measurement dashboard, source-yield report, archive helper, shadow conviction priors, and paper-risk research. Expanded append-only feedback labels and kept all paths research-only/no-send/no-live-by-default. |
| done | Add optimized test iteration targets | Codex | Kept strict `make verify` unchanged, added `make verify-fast`, `make test-pytest-durations`, xdist-backed safe `make test-pytest-parallel`, and documented when to use each path. `pytest-xdist` is now a declared dev dependency so bootstrapped environments can parallelize. |
| done | Define Event Alpha Radar North Star operating contract | Codex | Added `research/EVENT_ALPHA_RADAR_NORTH_STAR.md/json`, `make event-alpha-radar-north-star`, and project-health warnings/blocker checks for missing contract docs, missing burn-in contract, and `auto_apply_thresholds=true`. The contract defines radar components, six lanes, bounded human labeling, 30-day no-send burn-in minimums, and source activation order without changing product behavior. |
| done | Archive historical refactor reports and hide legacy aliases | Codex | Moved historical `REFACTOR_*` / `FINAL_REFACTOR_*` reports to `research/archive/refactor_history/`, removed root duplicate final reports, added an archive README/manifest, kept old legacy CLI spellings hidden/deprecated, and made `architecture-naming-check` block active refactor-era reports outside the archive. Current reports remain `ARCHITECTURE_*` / `PROJECT_HEALTH_*`. |
| done | Complete architecture naming cleanup | Codex | Retired old `make refactor-*` aliases, stopped current tools from writing old report aliases, tightened `make architecture-naming-check` so current-use retired terminology blocks, and regenerated canonical `ARCHITECTURE_*` / `PROJECT_HEALTH_*` reports. Historical refactor-era reports now live under `research/archive/refactor_history/`. |
| done | Make project-health architecture tooling permanent | Codex | Moved top-level static architecture tooling into `crypto_rsi_scanner/project_health/`, made `architecture-*` Make targets canonical, added `make architecture-naming-check`, and changed generated reports to canonical `ARCHITECTURE_*` / `PROJECT_HEALTH_*` names. |
| done | Clean up final architecture terminology | Codex | Added the transitional-file checker and naming checker, removed current-use migration wording from source/tests/docs, and preserved historical-artifact CLI/report semantics without exposing removed Make target aliases. |
| done | Complete final architecture acceptance audit | Codex | Added the exact final acceptance decision and completion-map architecture gates summary so the completion map exposes zero transitional files, zero old-path imports, zero nonessential shims, zero retained public entrypoints, canonical import coverage, and the `event_fade.py`/`scanner.py` exceptions. |
| done | Finish refactor sealing verification and report CLI dispatch fix | Codex | Fixed the final Event Alpha command-registry mismatch where impact-hypothesis and incident reports still read stale `args.include_api`; parser-backed tests now cover historical-artifact dispatch. Full sealing verification passed: standalone `760/760`, safe pytest `777 passed`, compileall, old-import/shim/size/class/final reports, integrated radar doctor, export smoke (`bad_entries=0`), Catalyst-frame e2e, and `make verify`. |
| done | Seal refactor v3 reporting, shim retirement, and export/cache hygiene | Codex | Reconciled final/size/class/RC reports around `accepted_with_documented_exceptions`, keeping `v3_blockers=[]` and accepted exception detail for 13 over-1,200-line files plus 3 storage mixins. No old flat Event Alpha public compatibility shims remain (`retained_public_shims_count=0`, `active_shims=0`, `deleted_shims=124`); deleted imports are tombstoned. Export archives clamp future mtimes, validate Makefile/archive timestamps, and exclude cache dirs; shim scan caching reports explicit future-mtime misses and steady-state dependency scans hit the cache while runtime artifacts stay skipped by default. |
| done | Harden ops CLI regression gate | Codex | Restored wrong-depth relative imports in the legacy RSI CLI service, added subprocess smokes for `--status`/backup/restore/log rotation plus static relative-import integrity coverage, added `pytest>=8.0`, and made `make verify` depend on hard-failing `make test-full` so the pytest-compatible package suite cannot be silently skipped. |
| done | Scope Event Alpha shim scans to source by default | Codex | Added a source-scoped shim scanner with runtime-artifact exclusions, file-size limits, cache reuse, force-rescan/runtime opt-in flags, scan accounting, doctor warnings, and focused tests. Current `research/ARCHITECTURE_FINAL_REPORT.json` records `shim_dependency_report_cache_status=hit`, `shim_dependency_include_runtime_artifacts=false`, and `shim_dependency_skipped_artifact_files=1285` without parsing runtime artifacts by default. |
| done | Accept Event Alpha refactor v3 release candidate | Codex | Full 26-command safe gauntlet passed and `research/REFACTOR_V3_RELEASE_CANDIDATE_REPORT.md/json` records `acceptance_status=accepted`, `critical_gate_status=pass`, `nonessential_shims_remaining=0`, `old_path_internal_imports=0`, `production_files_over_1500_lines=0`, `functions_over_150_lines=0`, and `unknown_namespace_count=0`. Remaining over-1,200 files and storage mixin classes are accepted warnings with owner notes. |
| done | Document final Event Alpha shim surface | Codex | Re-ran shim dependency and old-import checks with `old_path_internal_imports=0`, `old_path_test_imports=0`, `old_path_docs_references=0`, and `old_path_import_allowed_exceptions=0`. The retained public shim surface is empty; deleted old imports are tombstoned, `tests/event_alpha/test_no_old_event_alpha_imports.py` covers failure of deleted paths, and artifact doctor warns if a deleted shim file is reintroduced. |
| done | Tighten v3 production size gates | Codex | Split the low-risk near-threshold Event Alpha pipeline, Coinalyze preflight, and legacy CLI utility surfaces; updated size/final/completion reports so production files over 1,200 lines are accepted warnings and files over 1,500 lines are blockers. Current counters: `production_files_over_1500_lines=0`, `production_files_over_1200_lines=12`, `accepted_production_files_over_1200_lines=12`, `unresolved_production_files_over_1200_lines=0`, `functions_over_limit_count=0`, and completion map `status=accepted`. |
| done | Register multi-class model bundles in refactor gates | Codex | Added explicit accepted model-bundle metadata and changed class ownership gates so only unregistered multi-class production modules count as unresolved. Current counters: `multi_public_class_modules_count=80`, `accepted_model_bundles_count=79`, `unresolved_multi_class_modules_count=0`, `modules_with_multiple_public_classes_count=0`, `public_classes_not_in_own_module=0`, and `functions_over_limit_count=0`. |
| done | Reduce class ownership debt to storage mixin exceptions | Codex | Split provider and LLM shells from support/client/parser/model code, moved `CanonicalAsset` to its own module, and regenerated refactor reports. Current counters: `classes_over_limit_count=3`, `functions_over_limit_count=0`, `remaining_class_ownership_debt_count=0`, and every remaining oversized class is an accepted storage mixin exception (`SignalsMixin`, `WatchlistMixin`, `MigrationsMixin`) with DB-safety revisit conditions. |
| done | Retire remaining nonessential Event Alpha shims | Codex | Removed 30 additional non-public old flat shims after updating Makefile entrypoints, docs, tests, module maps, and compatibility assumptions to canonical package paths. Current counters: `deleted_shims=115`, `active_shims=9`, `retained_public_shims_count=9`, `nonessential_shims_remaining=0`, `old_path_internal_imports=0`, `old_path_test_imports=0`, `old_path_docs_references=0`, and `old_path_import_allowed_exceptions=19`. Only documented public compatibility wrappers plus `event_fade.py` remain. |
| done | Retire first non-public Event Alpha shim batch | Codex | Removed 85 non-public old flat shims that had zero internal, Makefile, script, dynamic, and artifact-doc references and were only exercised by the dedicated compatibility test. Added `research/EVENT_ALPHA_DELETED_SHIMS.md/json`, import-failure coverage for deleted paths, and regenerated shim/refactor reports. Current counters: `deleted_shims=85`, `active_shims=39`, `top_level_event_module_count=40`, `old_path_internal_imports=0`, `old_path_test_imports=0`, `old_path_docs_references=0`, `old_path_import_allowed_exceptions=49`. |
| done | Enforce canonical Event Alpha internal imports | Codex | Added `make event-alpha-old-import-check`, `research/EVENT_ALPHA_OLD_IMPORT_CHECK.md/json`, doctor/refactor counters, and a dedicated legacy import compatibility test module. Current counters after the first deletion pass: `old_path_internal_imports=0`, `old_path_test_imports=0`, `old_path_docs_references=0`, `old_path_import_allowed_exceptions=49`. Ordinary tests and internal code now import canonical package paths; retained shims remain available only where documented. |
| done | Define Event Alpha refactor v3 finalization contract | Codex | Added `research/REFACTOR_V3_CONTRACT.md/json` and threaded v3 gates through final, size, class ownership, and shim dependency reports. Current v3 is intentionally pending after the first deletion pass: `deleted_shims=85`, `nonessential_shims_remaining=26`, `old_path_internal_imports=0`, `public_compatibility_shims=13`, `shim_removal_blockers=26`, `production_files_over_1200_lines=15`, `production_files_over_1500_lines=0`, `public_classes_not_in_own_module=82`, `class_exceptions_remaining=14`, `functions_over_150_lines=0`, `old_path_docs_references=0`, `old_path_test_imports=0`, and `old_path_import_allowed_exceptions=49`. |
| done | Finish production-size refactor burn-down | — | Remaining large production modules were split into focused same-name packages while preserving compatibility exports and old imports. `make refactor-size-gates` now reports `production_size_gate_status=pass` with `production_files_over_1500_lines=0`, `production_files_over_2000_lines=0`, and `production_files_over_3000_lines=0`; `research/REFACTOR_COMPLETION_MAP.md/json` and `research/REFACTOR_RELEASE_CANDIDATE_REPORT.md/json` are accepted from the 23-command safe regression run. Remaining refactor debt is advisory class/function ownership (`29` classes and `56` functions over limits), not production file size. |
| done | Decompose Event Alpha legacy implementation cores and enforce legacy-aware gates | — | `impact_hypotheses/legacy.py`, `validation/legacy.py`, `core/store_api.py`, `evidence/acquisition_api.py`, `discovery/legacy.py`, `watchlist/legacy.py`, and `near_miss/legacy.py` are now sub-60-line compatibility binders over focused model/store/loader/executor/serialization/report/helper modules. `make refactor-size-gates` passes with 0 new violations, no legacy file over 3,000 lines remains, and `research/REFACTOR_COMPLETION_MAP.md/json` plus `research/REFACTOR_RELEASE_CANDIDATE_REPORT.md/json` are accepted from the 23-command safe regression run. |
| done | Accept Event Alpha refactor v2 release candidate | — | `research/REFACTOR_RELEASE_CANDIDATE_REPORT.md/json` and `research/REFACTOR_COMPLETION_MAP.md/json` are accepted after the full 27-command safe regression gauntlet. `scanner.py` is now a 77-line compatibility facade, `cli/services/event_alpha.py` is 46 lines, public artifact doctor is 41 lines, active-shim implementation violations are 0, `legacy_unregistered=0`, and no live calls/sends/trading/paper/RSI/Event Alpha `TRIGGERED_FADE` side effects were introduced. |
| done | Split CLI parser and registry-back Event Alpha dispatch | — | `build_parser()` is now a 25-line orchestrator over parser extension modules, `commands_event_alpha.handle()` is a 2-line registry bridge, `research/CLI_FLAG_SNAPSHOT.json` captures 235 long-form flags/defaults/action types/destinations/groups, and registry tests verify Event Alpha command metadata imports plus no-live/no-send defaults. Scanner body extraction and service bind-site reduction are now covered by the v2 acceptance row. |
| done | Split shared storage/backtest/schema facades and add progressive size gates | — | `storage.py` is now a 34-line `Storage` facade over `storage_parts/`, `backtest.py` is a compatibility facade over `backtest_parts/`, and `event_alpha/artifacts/schema_v1.py` is an 11-line schema v1 aggregator over `event_alpha/artifacts/schema/`. Added `make refactor-size-baseline-update` and `make refactor-size-gates`; the checked-in baseline records current large-file/function/class/module ownership violations as warnings, while the gate blocks newly introduced violations. |
| done | Extract scanner-owned Event Alpha command bodies | — | `crypto_rsi_scanner/scanner.py` is now a 77-line compatibility facade over `crypto_rsi_scanner/cli/services/scanner_api.py`, preserving old imports and monkeypatch-heavy tests while moving the command brain under `crypto_rsi_scanner.cli`. The transitional `scanner_api.py` core is tracked in `research/REFACTOR_COMPLETION_MAP.md/json` for later focused burn-down. |
| done | Finish Event Alpha module migration/class ownership inventory | — | Moved the last Event Alpha-specific implementation holdouts (`event_alpha_missed.py`, `event_alpha_reason_text.py`) into package homes, moved shared `event_clock.py`/`event_models.py` into neutral `event_core/`, and kept all old paths as active shims. Current `research/REFACTOR_FINAL_REPORT.md/json`: `scanner.py` 77 lines, `cli/services/event_alpha.py` 46 lines, public package doctor 41 lines, legacy doctor compatibility core 6,363 lines, `tests/test_indicators.py` 1,771 lines, active shims 124, partial shims 0, unmigrated modules 1 (`event_fade.py` intentionally outside Event Alpha), unknown namespaces 0, `legacy_unregistered=0`, and 0 active-shim implementation violations. `make refactor-class-ownership-report` writes the static class/function ownership inventory. |
| done | Split artifact doctor public orchestrator | — | `crypto_rsi_scanner/event_alpha/doctor/artifact_doctor.py` is now a 41-line public orchestrator/export surface over `context.py`, `discovery.py`, `execution.py`, `aggregation.py`, model modules, report section modules, and `checks/secrets.py`. `legacy_unregistered=0` from the public registry source path, output behavior stays delegated to the compatibility core, and `research/REFACTOR_FINAL_REPORT.md/json` records `legacy_artifact_doctor_core_lines=6363` as the next safe internal burn-down target. |
| done | Split largest Event Alpha internal wrappers | — | Added small public wrappers/packages for notification pipeline, research cards, daily brief, integrated radar, impact hypotheses, core opportunity store, and evidence acquisition. Focused module homes now exist for models, renderers, writers, selectors, sections, serialization, validators, and reports. Behavior-compatible cores remain tracked as `pipeline_api`, `research_cards.legacy`, `daily_brief.legacy`, `integrated.legacy`, `impact_hypotheses.legacy`, `core.store_api`, and `evidence.acquisition_api`; `research/REFACTOR_FINAL_REPORT.md/json` records wrapper/core line counts. |
| done | Split medium Event Alpha radar and provider packages | — | Validation, discovery, watchlist, near-miss, CryptoPanic, Coinalyze, provider-health wrappers, and low-risk Bybit/Binance announcement adapters now have focused package/module homes with old imports preserved. CryptoPanic text redaction was tightened for existing token-diagnostic hygiene, and no-call/no-send provider behavior remains unchanged. |
| done | Burn down scanner legacy CLI compatibility core | — | The scanner compatibility core is now a 120-line aggregator over `cli/services/scanner_parts/` modules. The root scanner facade now syncs monkeypatches through split-module wrappers so old tests and imports continue to work. |
| done | Burn down legacy doctor compatibility core | — | The artifact-doctor compatibility core is now a 95-line aggregator over `event_alpha/doctor/artifact_doctor_parts/` modules. Counter names, strict/WARN semantics, stale namespace handling, and old result exports are preserved. |
| done | Burn down remaining large Event Alpha compatibility cores | — | The prior Event Alpha legacy blockers are split into focused modules and compatibility binders. The final report now records `legacy_decomposition_gate_status=warning`, `legacy_files_over_3000_lines=0`, `legacy_files_over_1500_lines=1`, and `legacy_total_lines=6728`; no completion blocker remains for this pass. |
| done | Document remaining class/function ownership debt | — | No production file-size blockers or oversized functions remain. The remaining `14` oversized classes are explicitly recorded as accepted exceptions with owner notes and revisit conditions in `research/REFACTOR_CLASS_OWNERSHIP_REPORT.md/json`, and the reports now show `accepted_class_exceptions_count=14`, `remaining_class_ownership_debt_count=0`, `functions_over_limit_count=0`, and `new_violation_count=0`. Revisit only when the documented provider, storage, LLM, or schema conditions trigger; do not churn stable behavior just to satisfy the advisory 75-line class threshold. |
| todo | Keep provider/backtest compatibility binders shallow | Codex/open | Future provider/backtest/schema work should continue using focused package modules and thin compatibility binders. Preserve `python -m crypto_rsi_scanner.backtest`, historical helper monkeypatching, fixture output text, SQLite schema/migrations, paper-book behavior, artifact schema ids, doctor schema validation semantics, provider request ledgers, and no-live defaults. |
| done | Accept Event Alpha refactor v1 baseline release candidate | — | The earlier v1 baseline was accepted after the full post-refactor gauntlet. Refactor v2 is now separately accepted above; provider activation remains behind the behavior freeze. |
| done | Generate refactor final gate report | — | Added canonical `research/REFACTOR_FINAL_REPORT.md/json` plus `make refactor-final-report` to measure final refactor gates. The report records safe pytest plugin mode, namespace unknown count, CI static safety, scanner bind call sites, remaining scanner command bodies, split Event Alpha service line counts, and documented legacy-core details. Current legacy-aware final gate has no blockers; the remaining legacy decomposition state is a warning tracked by progressive size gates. |
| done | Enforce Event Alpha compatibility shims | — | Added `crypto_rsi_scanner.event_alpha.shims` as the active/partial/not-migrated shim registry, `make event-alpha-shim-report` for JSON/Markdown audit artifacts, tests for active-shim and partial-shim behavior, and an artifact-doctor warning when active shims contain implementation logic. Current shim state: 124 active shims, 0 partial shims, 1 intentionally outside top-level implementation (`event_fade.py`), and 0 active-shim logic violations. |
| done | Prepare Event Alpha shim retirement readiness | — | Added `make event-alpha-shim-dependency-report`, `research/EVENT_ALPHA_SHIM_DEPENDENCY_REPORT.md/json`, `research/EVENT_ALPHA_SHIM_REMOVAL_CANDIDATES.md/json`, and `research/EVENT_ALPHA_SHIM_RETIREMENT_POLICY.md`. Internal implementation imports now use new package paths; the dependency report records `internal_import_reference_count=0`, `test_import_reference_count=127`, `makefile_reference_count=8`, `dynamic_import_reference_count=0`, `safe_to_remove_count=0`, and no removal-now candidates in this no-deletion phase. |
| done | Document Event Alpha v1 architecture guardrails | — | Architecture, schema, namespace lifecycle, consolidation, runbook, AGENTS, and DECISIONS now document the package map, old-import shim policy, CLI/test placement rules, schema-first doctor policy, namespace lifecycle requirements, research-only/no-trading/no-paper/no-send safety invariants, and how to add providers, radar artifacts, notification lanes, and outcome/calibration fields. Static CLI tests now guard those docs against dropping the v1 anti-sprawl contract. |
| done | Harden GitHub Actions safe verification | — | `verify.yml` now runs standalone tests, full pytest, compileall, and `make verify` on push/PR with alerts disabled and no secret/live-provider env. `event-alpha-smoke.yml` remains manual `workflow_dispatch` only and runs fixture/no-call Event Alpha smoke targets. Static CLI tests guard workflow command order, no secret references, no live allow flags, and no live-send targets. README documents the no-live/no-send/no-trading CI boundary. |
| done | Make artifact namespace lifecycle first-class | — | Namespace lifecycle now writes `event_alpha_namespace_registry.json`, `event_alpha_namespace_lifecycle_report.md`, and per-namespace `event_alpha_namespace_status.json` markers with active fixture/provider/rehearsal, stale, archive, quarantine, and unknown statuses. The old `notify_llm_deep` namespace is marked `stale_deprecated`, stale/archived/quarantine namespaces are blocked for send-readiness, strict doctor short-circuits stale namespaces without treating legacy rows as current schema regressions, and archive planning remains dry-run only. |
| done | Move major artifact doctor checks into plugins | — | Added `crypto_rsi_scanner/event_alpha/doctor/checks/` plugins for integrated radar/core/card, provider readiness/preflight, source coverage, notification delivery/preview, path hygiene, and outcome/quality/incident message checks while keeping `event_alpha_artifact_doctor.py` as the compatibility orchestrator. The monolith is down to 6,335 lines and the registry `legacy_unregistered` ceiling is now 15; remaining namespace/stale/safety placeholders are ready for narrower follow-up migrations. |
| done | Add schema-dependent doctor check registry | — | Expanded the Event Alpha doctor check registry with declared check ids, categories, severities, schema dependencies, descriptions, schema version metadata, registry summaries, and a legacy-unregistered append-site acceptance gate. Doctor reports now include a “Doctor Check Registry” section, schema/safety findings begin using registry ids in output, and `make event-alpha-doctor-check-registry` validates registry dependencies plus the `legacy_unregistered=182` ceiling. |
| done | Make Event Alpha artifact doctor schema-first | — | Added namespace, schema, schema-safety, consistency, and report phase boundaries under `crypto_rsi_scanner/event_alpha/doctor/` while keeping the top-level doctor as the compatibility CLI entrypoint. Doctor output now reports stable schema counters, `--event-alpha-doctor-schema-only` can stop after namespace/schema/safety validation, `--event-alpha-doctor-skip-legacy-checks` exists for development only, and legacy imperative checks still run by default. |
| done | Finish Event Alpha artifact schema v1 stamping | — | Expanded `schema_v1.py` to declare the current v1 artifact contracts, validation helpers, filename inference, secret/path/safety checks, and safe stamping helpers. Key integrated radar, derivatives, market anomaly, official exchange, scheduled catalyst/unlock, provider readiness/preflight, notification delivery, core opportunity, outcome, namespace, and run-ledger writers now add `schema_id` where safe while preserving legacy writer-specific `schema_version` strings. `research/EVENT_ALPHA_ARTIFACT_SCHEMA_V1.md` records the freeze policy that future doctor checks must update schema dependencies first. |
| done | Split RSI/backtest/paper/CLI tests into pytest packages | — | Moved 105 remaining base tests from `tests/test_indicators.py` into `tests/rsi/` and `tests/cli/`, added focused `make test-rsi`, `make test-cli`, full `make test-pytest`, and xdist-aware `make test-pytest-parallel` targets. The standalone runner now reports 717 tests total with 97 RSI package tests, 17 CLI package tests, 559 Event Alpha package tests, and 44 umbrella tests; `tests/test_indicators.py` is down to 1,770 lines, below the final-phase 2k gate. `research/TEST_SUITE_ORGANIZATION.md` records the current layout and remaining legacy sections. |
| done | Split Event Alpha tests into pytest package | — | Moved 537 Event Alpha tests from `tests/test_indicators.py` into focused `tests/event_alpha/` modules for integrated radar, notifications, provider readiness, artifact doctor/schema, outcomes, source coverage, and namespace lifecycle. The standalone runner remains compatible by script-loading the split modules, now reports 708 tests total with 559 Event Alpha package tests, restores config state per test, and `make test-event-alpha` runs `python -m pytest tests/event_alpha`. `tests/test_indicators.py` is down to 4,987 lines and 149 local base/legacy tests. |
| done | Move Event Alpha outcome/calibration modules into package homes | — | Integrated radar outcomes, calibration, eval export, feedback readiness, burn-in scorecard/readiness/pack, quality review/coverage, signal-quality/export/tuning, priors, and policy simulation now live under `crypto_rsi_scanner.event_alpha.outcomes`. Top-level modules remain quiet compatibility shims, focused pytest coverage checks old/new imports plus outcome/calibration/feedback no-send behavior, output terminology such as `validation_rate` and `validated`/`invalidated`/`noise`/`inconclusive` is preserved, and the current size gate is 125 top-level `event_*.py` files: 56 shims and 69 remaining implementation files. |
| done | Move Event Alpha notification modules into package homes | — | Notification planning/preview/send orchestration, delivery ledger, sender metadata, run summaries, go/no-go, checklist, inbox, export pack, pause, SLO, send-readiness, Telegram final-check, recipient-check, and formatting facade now live under `crypto_rsi_scanner.event_alpha.notifications`. Top-level modules remain quiet compatibility shims, focused pytest coverage checks old/new imports, no-send preview/final-check behavior, delivery `status`/`status_detail`, and heartbeat wording, and the current size gate is 125 top-level `event_*.py` files: 42 shims and 83 remaining implementation files. |
| done | Move Event Alpha provider/readiness modules into package homes | — | Live-provider readiness, Coinalyze preflight/rehearsal orchestration, official exchange activation/normalization, CryptoPanic preflight support, provider health, source registry, source packs, Bybit announcements preflight, unlock/calendar preflight, and DEX/on-chain readiness now live under `crypto_rsi_scanner.event_alpha.providers`. Top-level modules remain quiet compatibility shims, reusable lower-level adapters stay in `event_providers/`, `derivatives_providers/`, and `supply_providers/`, focused pytest coverage checks old/new imports and no-call smokes, and the current size gate is 125 top-level `event_*.py` files: 29 shims and 96 remaining implementation files. |
| done | Move Event Alpha radar modules into package homes | — | Integrated radar, market state/reaction/anomaly, CoreOpportunity aggregation/store, evidence acquisition, source coverage, opportunity verdict, impact hypotheses/store, and incident-store implementations now live under `crypto_rsi_scanner.event_alpha.radar`. Top-level modules remain quiet compatibility shims, focused pytest coverage checks old/new import equivalence and fixture lane counts, and the current size gate is 125 top-level `event_*.py` files: 18 shims and 107 remaining implementation files. |
| done | Move Event Alpha artifact modules into package homes | — | Artifact context/filtering, portable path helpers, run ledger, retention, and run-lock implementations now live under `crypto_rsi_scanner.event_alpha.artifacts`, with namespace status under `crypto_rsi_scanner.event_alpha.namespace.status`. Top-level modules remain quiet compatibility shims, and focused import tests verify old/new paths resolve equivalent callables without changing artifact paths or schemas. |
| done | Move scanner dispatch into CLI modules | — | `scanner.py` now stays as a compatibility wrapper for `cli()`/`main()` while `crypto_rsi_scanner.cli.main`, `dispatch.py`, and command-group modules route parsed args for RSI, paper, maintenance, export, Event Alpha, provider readiness, Coinalyze, Bybit/official exchange, and related research commands. Command bodies still bind to historical scanner helpers until later narrower moves, and `research/CLI_COMMAND_INVENTORY.md` records the size gate and handler map. |
| done | Mirror scanner parser into CLI facade | — | Added `crypto_rsi_scanner.cli.parser.build_parser()` as a compatibility mirror for the current argparse construction, plus command classification helpers and parser snapshot/help smoke coverage for RSI, backtest, paper, maintenance/export, integrated radar, artifact doctor, notification preview, provider readiness, Coinalyze, and Bybit/official exchange paths. Follow-up dispatch routing now lives in the CLI package while command bodies remain compatibility-bound to scanner helpers. |
| done | Record refactor baseline and behavior-freeze contract | — | Added `make refactor-baseline` plus `research/REFACTOR_BASELINE.md`/`.json` from a static inventory generator. The baseline records current major line counts, Event Alpha/CLI/test/workflow inventories, Event-related Makefile targets, namespace inventory, behavior-freeze compatibility rules, schema v1 artifact contract, and measurable final-phase gates before significant code movement. |
| done | Add Event Alpha consolidation scaffolding | — | Added the `crypto_rsi_scanner/event_alpha/` package skeleton with compatibility wrappers, `crypto_rsi_scanner/cli/` facade and command snapshots, schema v1 plus schema-doctor counters, namespace lifecycle registry/report/dry-run archive plan, pytest package scaffolding, and safe GitHub Actions verification workflows. Broad physical module moves remain future incremental work behind old/new import and smoke tests. |
| done | Add cross-run radar performance dashboard | — | Integrated radar outcome fill/calibration now writes `event_radar_performance_dashboard.md` and `event_radar_provider_performance.json` across selected namespaces, tracking maturation states, provider/source-pack/lane/market/crowding/source-strength performance, provider usefulness/noise, time-to-confirmation, and recommendation-only priors. Daily brief shows “Radar Learning Snapshot,” and artifact doctor blocks diagnostic leakage, auto-apply flags, missing low-sample warnings, and performance-dashboard wording drift. |
| done | Add DEX/on-chain/protocol fundamentals readiness scaffold | — | Added fixture/no-call readiness artifacts for GeckoTerminal-style DEX pools, CoinGecko DEX pools, and DefiLlama TVL/fees/revenue fundamentals. The scaffold writes DEX pool state, DEX anomaly, and protocol fundamental JSONL artifacts; classifies liquidity expansion, volume breakout, suspicious low-liquidity pumps, protocol growth, and fundamentals deterioration; links readiness into source coverage/live-provider readiness/integrated radar; and keeps low-liquidity DEX moves diagnostic unless market/liquidity gates pass. |
| done | Prepare structured unlock/calendar providers for activation | — | Added fixture-first/no-call preflight artifacts and provider-specific Make stubs for Tokenomist, Messari unlocks, and CoinMarketCal. The preflight rows expose configured status, env var names only, fixture/parser status, request-ledger paths, request budgets, supported event types, source packs, and zeroed no-send safety counters. Messari fixture parsing and unlock materiality metadata now feed scheduled catalyst/unlock-risk smokes, source coverage/readiness links the artifacts, and artifact doctor checks missing structured unlock evidence, missing event time/size metrics, live-without-ledger, side-effect claims, and secret leakage. |
| done | Turn market anomalies into a catalyst-search queue | — | Market anomaly scans now consume canonical asset registry metadata plus optional cached CoinGecko universe rows, rank broad market moves with liquidity/turnover/derivatives/source-unknownness/event-age components, bucket anomalies into high-liquidity breakout, stealth accumulation, late-momentum crowding-check, selloff risk, or low-liquidity suspicious diagnostics, and write `event_market_anomaly_catalyst_search_queue.jsonl`. Integrated radar keeps anomaly-only rows unconfirmed, lets official/structured evidence confirm valid market moves, and caps suspicious low-liquidity moves as diagnostics. |
| done | Add canonical asset/instrument resolver | — | Added a shared Event Alpha asset registry and instrument resolver that maps CryptoPanic-style tickers, CoinGecko IDs, official exchange pairs, Coinalyze futures symbols, and diagnostic themes to canonical identities. Integrated radar and official exchange artifacts now write registry/resolution reports and propagate `canonical_asset_id`, resolver confidence, and warnings into candidates, CoreOpportunity rows, and cards. Quote assets stay diagnostics unless explicitly targeted, BTC/ETH simple pair announcements stay capped, proxy assets are labeled proxy, SECTOR/theme rows are non-tradable diagnostics, and artifact doctor catches missing canonical IDs, quote/theme promotion, and unlinked Coinalyze symbols. |
| done | Add unified official exchange activation framework | — | Added shared official exchange activation JSON/Markdown over Bybit/Binance artifacts with distinct public/fixture versus signed-listener provider rows, source coverage/readiness/doctor integration, fixture-ready public parsing, signed listener blocked without env vars, and strict checks for live-without-ledger, source-field, major-pair, side-effect, and secret-leak violations. Defaults remain no-live-call and no-send. |
| done | Add Bybit announcement preflight and no-send rehearsal | — | Bybit official announcements now have provider-specific no-call preflight artifacts, a bounded no-send rehearsal with mandatory request ledger for any allowed live path, mocked/live rows normalized through the official exchange artifact pipeline, source coverage/readiness links, provider health states, and doctor checks for unsupported params, missing ledgers, side effects, secrets, missing source fields, and simple major-pair noise promoted to early-long. Defaults remain no-live-call and no-send. |
| done | Feed Coinalyze artifacts into integrated radar | — | Integrated radar can now load an explicit, readiness-discovered, or default Coinalyze no-send rehearsal namespace; attach derivatives state/crowding/fade-review rows to matching integrated candidates by symbol/coin id/market identity; surface Coinalyze health/freshness/source paths in manifests, source coverage, daily brief, and cards; and doctor hidden, stale, unattached, or silently skipped Coinalyze evidence. |
| done | Make Coinalyze derivatives metric claims explicit | — | Coinalyze preflight/rehearsal, derivatives state, crowding candidates, source coverage, cards, and artifact doctor now carry supported metric status, per-symbol freshness, and unit metadata. Predicted funding is fetched only through the documented bounded endpoint when request budget allows; basis is not claimed as live-implemented and remains fixture-only/planned until a provider endpoint is available. Strict doctor now catches implemented metric claims without state data, missing unit metadata, stale fade-review promotion, and card copy that shows predicted funding or basis without backing data. |
| done | Add Coinalyze preflight and bounded no-send rehearsal telemetry | — | Research-review previews/delivery rows now carry structured rendered/eligible/skipped counts, reason counts, display-level skipped summaries, raw candidate-family summaries, rendered candidate/core ids, and artifact-backed preview regeneration. Coinalyze has clean default artifact namespaces, a no-call preflight/smoke, and a guarded no-send live-capable rehearsal that remains blocked without key + explicit allow flag + ledger + tiny request budget; when live/mocked state rows exist, the rehearsal now writes deterministic derivatives crowding and fade/short-review research artifacts. Source coverage, daily brief, readiness, integrated radar manifests, and artifact doctor now agree on Coinalyze preflight/rehearsal artifacts only when files exist, stale namespaces block writes by default, provider health requires successful ledger plus derivatives state, and export archive mtimes/entries are validated for review portability. |
| done | Harden live-provider readiness telemetry and stale namespace handling | — | Integrated radar smoke now writes no-call live-provider activation readiness JSON/Markdown before source coverage, source coverage/daily brief link readiness artifacts or print the readiness command, research-review delivery rows carry rendered/eligible/skipped counts plus family-level skipped-candidate summaries, and stale namespaces can be explicitly marked so strict doctor ignores superseded artifacts unless legacy inspection is requested. |
| done | Add live-provider activation readiness and final preview cleanup | — | Event Alpha now writes no-call live-provider activation readiness JSON/Markdown artifacts, links them from source coverage/daily brief, recommends activation order for derivatives, official exchange, structured unlock/calendar, DEX/on-chain, protocol fundamentals, and context/news, and normal notification previews use strict-alert vs research/raw candidate wording with fresh preview-file rewrites. Operator command summaries use relative artifact paths by default, and the source-with-artifacts exporter clamps future mtimes to avoid review-zip Makefile clock-skew warnings. |
| done | Make integrated radar artifacts portable and outcome-aware | — | Integrated radar now writes portable operator Markdown paths, structured no-send preview delivery rows, preview/card/source coverage run-ledger telemetry, strict doctor checks for path leaks and delivery/outcome mismatches, and research-only outcome/calibration/prior artifacts. Outcome and calibration reports are recommendation-only and cannot send, trade, paper trade, write normal RSI rows, or create `TRIGGERED_FADE`. |
| done | Make integrated radar operator truth canonical | — | CoreOpportunity reloads, research cards, daily brief/card-index grouping, and artifact doctor now use canonical integrated candidate lane/why-now/market/derivatives truth instead of stale generic recomputation. BTC simple major-pair evidence stays unconfirmed in cards, TESTFADE/TESTPERP retain crowding/fade metadata, lane-first card groups are authoritative, and strict doctor checks catch visible card/core lane mismatches, hidden warnings, contradictory market/derivatives copy, mixed sidecar timestamps, and broad-source recommendations outranking lane-critical sources. |
| done | Harden integrated radar canonical artifact propagation | — | Integrated candidates now remain canonical at rest when written into CoreOpportunity rows, research cards, daily briefs, source coverage, and run ledger artifacts. Structured official exchange, scheduled catalyst, unlock, derivatives, market, source URL, reason/warning, route/state, score, and requirement fields are preserved; diagnostic rows are appendix-only by default; strict doctor checks catch silent upgrades, evidence loss, missing manifests/source-coverage JSON, card mismatches, and diagnostic leakage. |
| done | Add integrated Event Alpha radar cycle | — | Added a fixture-backed integrated cycle that runs/loads market anomaly, official exchange, scheduled/unlock, and derivatives/fade-review sidecars, merges them into `event_integrated_radar_candidates.jsonl`, writes canonical CoreOpportunity rows, research cards, source coverage, no-send notification preview, daily brief, and run-ledger artifacts, and enforces strict doctor checks for lane/source/market/fade/risk invariants. The cycle remains research-only and cannot create strict alerts, sends, normal RSI rows, paper rows, trades, execution, or Event Alpha `TRIGGERED_FADE`. |
| done | Stabilize Event Alpha market units and source-pack artifacts | — | Added a central market-return unit contract so raw/latest snapshots stay fractional while market-state artifacts persist percentage points with unit metadata; cards/briefs now format human percentages and doctor checks catch double-scaled market-state returns. Evidence acquisition rows are reconciled/capped when unresolved or rejected evidence would otherwise retain stale validated levels, derivatives scans now persist all evaluated candidates for doctor/audit while keeping fade-review subset artifacts, market anomaly rows carry `market_state_class`, and simple BTC/ETH/stable major-pair exchange announcements are capped to unconfirmed/diagnostic unless explicitly opted in. |
| done | Add Event Alpha derivatives crowding review lane | — | Added artifact-only derivatives state normalization and `FADE_SHORT_REVIEW` candidate artifacts for OI/funding/liquidation crowding review, with fixture smokes, daily-brief/card/notification copy, provider-health/run-ledger rows, and strict doctor checks. The lane is manual research metadata only and cannot create `TRIGGERED_FADE`, sends, trades, paper rows, or normal RSI rows. |
| done | Add Event Alpha scheduled catalyst and unlock pack | — | Added research-only scheduled catalyst/unlock normalization for Tokenomist-style unlocks and CoinMarketCal-style events, with JSONL/report artifacts, fixture smokes, daily-brief/card display, provider-health/run-ledger rows, and strict artifact-doctor checks. Unlock/supply strict lanes now require structured/official/supply proof, event time, source URL, and materiality; media-only unlock mentions remain context or diagnostics. |
| done | Build Event Alpha official exchange announcement pack | — | Added artifact-only official exchange normalization for Binance/Bybit fixture announcements, dedicated official listing/perp/risk source packs, daily-brief/card/audit display, source-coverage/provider-health reporting, strict artifact-doctor checks, and `make event-alpha-official-exchange-smoke`. Official exchange rows remain research-only and cannot create alerts, sends, trades, paper rows, normal RSI rows, or `TRIGGERED_FADE`. |
| done | Add Event Alpha market-state anomaly artifacts | — | Added a stable MarketStateSnapshot schema and a research-only broad market anomaly scanner that writes market-state snapshots, anomaly seeds, and a Markdown report under the selected namespace. Daily briefs now show “Market Anomalies Without Confirmed Catalyst,” and strict artifact doctor checks malformed anomaly artifacts without allowing anomalies to create alerts, sends, trades, or `TRIGGERED_FADE`. |
| done | Split Event Alpha weak lanes from risk-only | — | Opportunity-lane semantics now include `UNCONFIRMED_RESEARCH` and `DIAGNOSTIC`, so missing evidence/rejected evidence/source-only narratives no longer fall into `RISK_ONLY`. Daily briefs group canonical core rows by lane, cards/previews/audits show the same lane metadata, and strict doctor blocks `RISK_ONLY` as a generic missing-evidence bucket or diagnostic rows leaking into alertable/default lanes. |
| done | Add Event Alpha market reaction opportunity lanes | — | CoreOpportunity rows now carry pure research-only market-state snapshots, market reaction states, and explicit opportunity types (`EARLY_LONG_RESEARCH`, `CONFIRMED_LONG_RESEARCH`, `FADE_SHORT_REVIEW`, `RISK_ONLY`). Cards render why-now/evidence/confirm/invalidate/not-alertable text, and artifact doctor blocks impossible lane combinations such as confirmed-long without source+market confirmation, fade-review without crowding/exhaustion evidence, early-long without a fresh strong source, CryptoPanic-only narratives in confirmed lanes, or missing market snapshots. |
| done | Finish CryptoPanic integration consistency | — | Source coverage now reconciles successful CryptoPanic Growth requests against stale provider-health backoff, uses `observed_*` coverage states, and suppresses configure/restore-token recommendations once successful requests are observed. Evidence acquisition rows persist complete accepted/rejected arrays with canonical count/provider/reason-code summaries, artifact doctor checks count mismatches, stale CryptoPanic recommendations, and raw CoreOpportunity final-level contradictions. Raw core-store final fields are normalized to post-live-confirmation/post-quality truth, daily brief operator sections use canonical post-policy routes by default, research-card/index grouping collapses support duplicates by asset family, and live-style narrative daily digests require stronger confirmation than a single source-only fan/proxy/political row unless explicitly opted in, even when stale support rows carry direct-event source packs. |
| done | Harden CryptoPanic live diagnostics and research-review rendering | — | CryptoPanic live fetches now read/decode/parse bodies inside the response context, ledger parse/HTTP diagnostics with redacted body excerpts, classify parse/rate-limit/backoff/empty/server/network failures, and surface usable vs parse-error coverage states. Research-review Telegram bodies now prefer canonical CoreOpportunity cards and feedback targets, and strict doctor blocks body/core mismatches. |
| done | Harden CryptoPanic request planning and digest eligibility | — | CryptoPanic live requests are now validated ticker-only, reject slugs/sector/common-word/global requests, dedupe/cache normalized request keys within a run, and expose request-planning counters in ledgers/doctor checks. Live-style daily digest lanes now require confirmed evidence, group/cap items, move unconfirmed rows to research-review/local diagnostics, and delivery rows use structured identity arrays. |
| done | Fix CryptoPanic Growth Weekly request model | — | CryptoPanic live fetching now defaults to `https://cryptopanic.com/api/growth_weekly/v2/posts/`, reads the canonical `RSI_EVENT_DISCOVERY_CRYPTOPANIC_API_TOKEN` with legacy aliases, sends only Growth-supported params (`auth_token`, `public`/`following`, `currencies`, `regions`, `filter`, `kind`, `page`), batches validated currency queries, omits Growth-unsupported `search`/`size`/pull/content params, records a redacted request ledger, enforces weekly/run/day quota caps, and surfaces quota/unsupported-param/token-leak checks in preflight, source coverage, run ledger, and artifact doctor. |
| done | Prove CryptoPanic in notify_llm_deep no-send path | — | Added a redacted CryptoPanic preflight, a `notify_llm_deep_cryptopanic_rehearsal` no-send target, run-ledger CryptoPanic counters, source-coverage CryptoPanic summary, token-redacted live-provider failure handling, and artifact-doctor checks for CryptoPanic coverage/card/promotion issues. |
| done | Fix research-review daily brief namespace selection | — | Daily brief/report Make targets now include test artifacts when the inspected `ARTIFACT_NAMESPACE` is a smoke namespace, so `PROFILE=notify_llm_deep ARTIFACT_NAMESPACE=notify_llm_deep_research_review_smoke` selects the current run/core rows. The deep research-review smoke now writes and checks source coverage plus daily brief artifacts, and strict doctor blocks broken brief selection, core-count, research-review-lane, or source-coverage-link mismatches. |
| done | Operationalize research-review source coverage | — | Source coverage report commands now separate runtime profile from artifact namespace, persist `event_alpha_source_coverage.md`/`.json` into the inspected namespace, and mark configured providers without health observations as `unknown/not observed` instead of healthy. Strict doctor surfaces missing/unknown coverage artifacts as diagnostics while preserving no-send/research-only behavior. |
| done | Reconcile deep rehearsal core-card coverage | — | Prompt 10 fresh-artifact audit found two latest-run `notify_llm_deep_rehearsal` canonical core rows hidden by aggregation and missing research-card paths/feedback targets. Card writing now appends any store-backed core rows omitted by aggregation, and fresh no-send rehearsal strict doctor/send-readiness pass with no blockers while sends remain disabled. |
| done | Harden Event Alpha feedback calibration fields | — | Feedback rows now fill top-level calibration dimensions from the matched canonical row/context when score components omit them, including `source_pack`, source class/domain, impact path, candidate role, opportunity level, evidence specificity, and market confirmation. Calibration and proposed signal-quality case export can now use useful/junk/watch rows without digging through nested audit metadata. |
| done | Polish Event Alpha burn-in review queue | — | The burn-in notification inbox now starts with a compact ranked review queue covering strict/high-priority/digest would-send rows, research-review near-misses, upgrade candidates, local-only learning rows, and diagnostics. Diagnostic/source-noise/ticker-collision controls are hidden by default, card labels use basenames only, and the no-send research-review smokes prove VELVET/AAVE/DOGE queue ordering without changing routes, scores, send guards, or `TRIGGERED_FADE`. |
| done | Add LLM analyst tools beyond catalyst frames | — | Event Alpha now has constrained source-triage validation, v2 evidence planning metadata, denial/correction checks, manual verification criteria, deterministic analyst summaries in cards, and explicit analyst-tool budget counters. These improve operator context only; deterministic source/identity/quality gates still decide final route/state and `TRIGGERED_FADE` remains event_fade.py-only. |
| done | Harden Event Alpha asset-role identity | — | Event Alpha now uses a lightweight deterministic asset knowledge graph for common core assets, venue tokens, fan/meme tokens, infrastructure tokens, stable/wrapped assets, and broad macro assets. Resolver, impact-path validation, watchlist/core rows, cards, audits, and daily briefs carry identity confidence/evidence, matched field, role source, asset kind, capabilities, and collision risk. Taxonomy-only suggestions remain diagnostic until source text directly validates the asset/mechanism. |
| done | Rebuild source enrichment and article-quality triage | — | Source enrichment now persists extractor/cleaner versions, fetch/final/canonical URLs, redirect chains, article metadata, body length, boilerplate/ticker-sidebar detection, quality status, deterministic triage, and source-quality score. Google News placeholders, blocked/thin pages, SEO/referral pages, market recaps, prediction-market context, and boilerplate-heavy articles are capped or diagnostic before LLM extraction/catalyst frames consume enriched text. Cards, audits, daily briefs, and source coverage show article-quality status when evidence carries enrichment metadata. |
| done | Add derivatives, DEX liquidity, and protocol metric confirmation | — | Market confirmation now exposes fresh/stale derivatives, DEX liquidity, and protocol-metric sub-confirmation fields. Coinalyze-style OI/funding/liquidations, GeckoTerminal-style pool liquidity/DEX volume, and DefiLlama-style TVL/fees/volume rows can support playbook-specific market confirmation while remaining unable to prove catalyst identity/impact by themselves. Cards, audits, daily brief, and quality review show these sub-confirmations and freshness gaps. |
| done | Promote structured calendar and unlock evidence packs | — | CoinMarketCal-style calendar rows now preserve event-time provenance, event category, confidence, original source URL, and token identity under `source_class=structured_calendar`; Tokenomist-style unlock rows preserve unlock type/category, materiality, normalized supply impact, and token identity under `source_class=structured_unlock`. Source packs now include `project_event_pack`, require material/non-stale unlock evidence for digest/watchlist promotion, keep low-authority calendar rows local/review-only, and show structured evidence details in cards/audits without weakening strict gates. |
| done | Make CryptoPanic and official exchange evidence first-class | — | CryptoPanic catalyst searches now request validated symbol plus coin slug, evidence rows preserve currency tags/tag-match/narrative-heat metadata, and hot/bullish-only or tag-mismatched rows reject explicitly. Binance/Bybit announcements now normalize spot/perp/delist/product events with exchange, symbol, pair/contract, timestamp, and URL metadata. Cards, audits, and daily briefs show compact source-class/tag/exchange proof without weakening strict alert gates. |
| done | Harden Event Alpha source-system coverage semantics | — | Source coverage rows now expose pack-level provider coverage status, role-specific provider health, explicit coverage-gap reasons, and missing/degraded confirmation providers. Artifact doctor now warns/blocks contradictory source-coverage metadata, especially when degraded/unavailable/not-configured coverage is treated as meaningful absence. |
| done | Operationalize research-review digest in notify_llm_deep rehearsals | — | Run ledgers now persist research-review digest enable/candidate/would-send/sent/block counters, strict doctor flags latest-run missing-lane regressions, and `make event-alpha-notify-llm-deep-research-review-no-send-smoke` proves a real-profile no-send rehearsal writes the research-review delivery row. Source coverage recommendations now include actionable provider/pack setup and repair guidance. |
| done | Add Event Alpha source coverage dashboard | — | Added `main.py --event-alpha-source-coverage-report` / `make event-alpha-source-coverage-report PROFILE=...` to summarize source-pack provider coverage, provider health, evidence-acquisition outcomes, meaningful absence, and coverage blockers. Daily briefs now name the source likely to most improve the next run. CoinGecko/DefiLlama are tracked as market/protocol metric coverage evidence without changing strict alert gates. |
| done | Add Event Alpha research-review notification lane | — | Added a separate `research_review_digest` lane for non-alertable exploratory/near-miss candidates during research burn-in. It has independent config/cooldown/dedupe, excludes strict-alertable and hard-gated garbage by default, labels Telegram copy as “Not alertable / Missing confirmation / Not a trade signal,” appears separately in previews, delivery ledger, inbox, and daily brief, and is covered by `make event-alpha-research-review-digest-smoke` plus strict artifact-doctor checks. |
| done | Add safe one-cycle Event Alpha Telegram re-enable path | — | Added `make event-alpha-telegram-one-cycle-send-preflight` as a no-send gate over the latest rehearsal artifacts; it writes a preflight marker only after the compact final check reports ready. Added `make event-alpha-telegram-send-one-cycle PROFILE=notify_llm_deep`, which refuses unless `RSI_EVENT_ALERTS_ENABLED=1`, Telegram config is present, and either `CONFIRM=1` or a preflight marker exists; it reruns the compact rehearsal final check immediately before the guarded send. Added `make event-alpha-telegram-post-send-audit` and an `event-alpha-notification-pause` alias for immediate audit/kill-switch workflow. |
| done | Make final Event Alpha Telegram no-send check deterministic | — | Added `main.py --event-alpha-telegram-final-check` as the compact read-only final gate over existing run, delivery, core, doctor, readiness, and go/no-go artifacts. `make event-alpha-telegram-no-send-final-check-fast` now rebuilds deterministic `notify_llm_deep_fixture_rehearsal` artifacts without recursive Make, captures noisy report output, and prints only status, preview path, strict doctor status, would-send lanes, core ids, send count, provider summary, and next commands. `make event-alpha-telegram-send-readiness-final` and `make event-alpha-telegram-final-send-checklist` use the same compact gate for existing namespaces and fail on `NOT_READY`; the existing live-provider rehearsal remains clearly labeled as slower and provider-dependent. |
| done | Finalize Event Alpha Telegram go/no-go UX | — | `--event-alpha-notify-go-no-go` is now artifact-backed and reports latest run id/completion, preview existence/source, explicit delivery status, canonical delivery identity, rejected/unconfirmed candidate selection, alertable item count, would-send lanes, and a final `READY_FOR_NO_SEND_REVIEW` / `READY_FOR_SEND` / `NOT_READY` recommendation. Notification previews have a concise summary and portable card-path labels. Added `make event-alpha-send-go-no-go` and `make event-alpha-telegram-no-send-final-check`; the deterministic deep fixture rehearsal now chains go/no-go, compact inbox, and daily brief checks without sending. |
| done | Make Event Alpha delivery ledger status explicit | — | Fresh notification delivery rows now persist explicit `delivery_mode`, `delivery_state`, `status_detail`, send-guard, would-send, sent, and failed fields in addition to legacy `state`. Strict artifact doctor and send-readiness block fresh latest-run rows with missing/contradictory delivery status, while old rows remain readable. The notification inbox has a compact `--event-alpha-burn-in-review` mode for phone-friendly review, and the deep no-send rehearsal now has an explicit deterministic fixture-candidate target plus tighter fast-rehearsal bounds. |
| done | Finish notify_llm_deep rehearsal send-readiness gate | — | Heartbeat/no-digest notification previews now source completed/raw-event/extraction/core-opportunity/alertable/provider/LLM counts from the latest run ledger and canonical core store instead of rendering stale zero summaries. Delivery rows persist portable `notification_preview_relpath` metadata, and readiness resolves preview paths by relpath, namespace default, then legacy absolute fallback so stale `/Users/...` paths do not block another checkout. Blocked/would-send rows distinguish no-send rehearsal, send-guard, quality, cooldown, and not-due states. `make event-alpha-send-readiness PROFILE=notify_llm_deep_rehearsal` is the final artifact gate before enabling Telegram: it requires a completed latest run, strict artifact doctor, matching preview summary, canonical core delivery identity, live-confirmed alertable items, clear send-guard status, resolvable preview path, and Telegram readiness for real sends. A capped `make event-alpha-notify-llm-deep-real-no-send-rehearsal-fast` target exists for quicker preview/readiness proof. |
| done | Harden live burn-in acquisition fail-soft behavior | — | Evidence acquisition now returns a complete run result for disabled/no-candidate/provider-failed paths, run ledgers expose `evidence_acquisition_run_status`, live burn-in no-send completes through GDELT/RSS provider failures, and readiness/doctor reports treat canonical core card mappings as the review source of truth. |
| done | Canonicalize Event Alpha notification identity | — | Routed Telegram notifications now reconcile through canonical CoreOpportunity rows before delivery, key delivery ledgers by `agg:...` core ids, preserve lower-level source alert ids as metadata, write multi-lane local notification previews plus no-candidate rehearsal previews, and block rejected-only/no-result/skipped-budget live digest items unless another real confirmation source exists. Artifact doctor now blocks current digest/escalation delivery rows missing canonical core id, feedback target, canonical card path, or canonical alert id, while latest-run scoped delivery checks label stale pre-core rows as legacy diagnostics unless an all-row sweep is requested. Broad BTC/ETH/SOL Strategy/MSTR treasury-valuation context is capped below digest without accepted/direct/fresh confirmation. Telegram digest copy is compact and hides raw debug fields/full local paths; fixture/no-send smokes exercise VELVET accepted high-priority, AAVE accepted digest, and BTC/TAO rejected controls, and `make event-alpha-notify-llm-deep-real-no-send-rehearsal` proves the actual deep profile with Telegram sends guarded off. |
| done | Make Event Alpha review surfaces core-first | — | Notification inbox, feedback readiness, opportunity audit, and artifact doctor now resolve operator review items through canonical CoreOpportunity rows first. Canonical cards and `agg:...` feedback targets win for VELVET-style opportunities, while source-noise/support alert snapshots stay hidden diagnostics unless explicitly requested. |
| done | Keep diagnostic alert snapshots separate from canonical core alerts | — | Diagnostic/support alert snapshots now keep `diagnostic_support_for_core_opportunity_id`, `is_diagnostic_snapshot`, and `snapshot_class=diagnostic_support_snapshot` while forcing final route/tier/alertability to store-only. They may carry a `support_for_core_summary` for audit, but only canonical-core snapshots inherit alertable core routes. Strict artifact doctor now reports support rows that become alertable, inherit core routes, or duplicate canonical alertable snapshots. |
| done | Reconcile alert snapshots with canonical core state | — | Alert snapshots now load/write through canonical CoreOpportunity final state when a `core_opportunity_id` resolves. Stale pre-cap snapshot fields such as `validated_digest` / `RESEARCH_DIGEST` are preserved as requested-before-reconciliation audit fields, while final route/state/level/alertability mirror the core row. Daily brief alertable counts, notification inbox queues, feedback readiness, opportunity audit, and strict artifact doctor use or enforce the reconciled final state. |
| done | Harden live/no-send digest promotion confirmation | — | Live-style Event Alpha core rows now require real confirmation before remaining `validated_digest` or above: accepted source-pack evidence, official/structured source evidence, CryptoPanic token/catalyst tag evidence, strong direct source evidence, or fresh non-generic market confirmation. `skipped_budget`, `no_results`, and `rejected_results_only` no longer validate by themselves; sector-only rows stay exploratory/local by default. Daily brief, quality review, and artifact doctor expose live confirmation caps. |
| done | Repair canonical CoreOpportunity route/verdict conflicts | — | Full post-implementation audit found live-style core rows with `final_opportunity_level=validated_digest` but `final_route_after_quality_gate=STORE_ONLY`. Core-store writes now derive final route/tier from the canonical opportunity level unless a real quality block, quality cap, duplicate suppression, or triggered-fade route applies, and strict artifact doctor blocks fresh core route/verdict conflicts. Fresh catalyst-frame, evidence-acquisition, market-refresh, quality-validation, and live no-send artifacts all report `core_route_conflicts_with_opportunity_level=0`. |
| done | Prove live-style Event Alpha no-send burn-in readiness | — | Added the `live_burn_in_no_send` profile, `make event-alpha-live-burn-in-no-send`, `make event-alpha-burn-in-readiness`, a provider readiness/source-pack gap summary, a daily-brief Burn-In Readiness section, and a strict readiness report that confirms fresh run success, no-send status, provider coverage, evidence acquisition, cards/feedback targets, market-freshness visibility, and artifact-doctor status before any notification promotion. |
| done | Surface source contracts in Event Alpha artifacts | — | Source Registry v2 now has a reusable source-contract helper that merges registry assessment with accepted/rejected evidence. Evidence-acquisition rows, research cards, and opportunity audits show what a source can prove, cannot prove, relevant playbooks, evidence absence semantics, and source coverage gaps. Non-official sources explicitly cannot prove official confirmation. |
| done | Join incidents into canonical CoreOpportunity views | — | The canonical core read model now joins profile-scoped incident rows and exposes the best incident/catalyst-frame row for cards/audits. Opportunity audit consumes that joined view so main catalyst frame, subject/actor/object, and evidence quote stay aligned with the canonical core opportunity rather than independently reconstructed support rows. |
| done | Make canonical CoreOpportunity rendering verdict-aware | — | Cards, opportunity audits, and artifact doctor now treat the canonical core row as the source for primary and secondary source/impact/market/upgrade copy. Accepted evidence can override filler `unknown` source text, promoted cores get verdict-aware upgrade/downgrade language, and support-row generic blockers stay diagnostic instead of contaminating high-priority card/audit sections. |
| done | Make canonical CoreOpportunity views acquisition-aware | — | Cards, opportunity audit, quality review, and artifact doctor now read accepted/rejected acquisition evidence through canonical core rows. VELVET/RUNE/ZEC accepted evidence appears in cards/audits, AAVE/MemeCore no-result source packs remain visible, quality review uses core rows as the primary operator view, and support/control rows are diagnostics only. |
| done | Audit Event Alpha fresh artifacts and card consistency | — | Full no-send QA sweep regenerated catalyst-frame, evidence-acquisition, market-refresh, quality-validation, and notify-quality-frame artifacts; strict doctors had no blockers; direct JSONL/Markdown checks found no remaining orphan core ids, final route/state contradictions, active weak watchlist rows, garbage canonical incidents, or invalid `TRIGGERED_FADE` paths. Fixed stale core-card quality copy so canonical cards use final quality-gated route/verdict fields instead of raw support-row digest gates. |
| done | Turn Event Alpha feedback into calibration data | — | Feedback rows now resolve watchlist/core/card/alert targets and persist core id, card path, run/profile/namespace, source pack/provider/domain, market freshness, catalyst-frame, final route/lane, and quality dimensions. Calibration, policy simulation, outcome fill, missed-opportunity rows, and signal-quality case export consume those fields for research-only learning loops without changing alert/trading behavior. |
| done | Harden Event Alpha source contracts and acquisition evidence | — | Source Registry v2 now exposes what each source can/cannot prove, useful playbooks, market-data source class, feed-level RSS quality/quarantine metadata, and coverage-gap semantics. Source packs now define validated-digest/watchlist/high-priority sufficiency criteria including `protocol_business_event_pack`; evidence acquisition rows persist attempted plans, execution results, query/provider coverage statuses, accepted/rejected evidence samples, and source-pack sufficiency fields. |
| done | Make Canonical CoreOpportunity the shared artifact read model | — | `event_core_opportunity_store.load_canonical_core_opportunity_view(...)` now returns the canonical core row plus linked supporting/diagnostic rows, evidence acquisition, market refresh evidence, research-card path, alert snapshots, and feedback status. Research cards and opportunity audits consult this read model first, so support/control rows remain attachments to the core opportunity rather than competing operator-facing rows. |
| done | Make canonical Event Alpha core rows authoritative for cards/snapshots | — | Research card generation, daily-brief card groups, alert snapshots, opportunity audit, and artifact doctor now resolve visible opportunities through canonical `event_core_opportunities.jsonl` rows when present. Card paths and feedback targets are written back onto stored core rows after card generation, and evidence-acquisition rows reconcile temporary/orphan core ids to compatible canonical cores with diagnostic-support metadata. Source-noise/control snapshots are diagnostic support with explicit support ids/status, not fake core opportunities. Market Freshness Readiness now separates core freshness from support gaps, and Source Coverage separates evidence plans created from acquisition requests executed. |
| done | Persist canonical Event Alpha core opportunities | — | Event Alpha cycles now append `event_core_opportunities.jsonl` under the active artifact namespace with one canonical post-refresh `CoreOpportunity` row per visible operator opportunity. Daily briefs, near-miss reports, research-card inputs, opportunity audit, run ledger metadata, and artifact doctor checks prefer this store when present, so promoted VELVET/RUNE-style cores do not reappear as stale support-row near-misses or downgrade-looking duplicates. |
| done | Make Event Alpha evidence acquisition final-verdict consistent | — | Evidence acquisition now separates evidence-quality changes from final opportunity upgrades. Acquisition artifacts persist `initial_*`, `post_refresh_*`, and canonical `final_opportunity_*` verdict fields, preserve stronger prior market-refresh verdicts when later evidence collection does not actually improve the opportunity, and keep market data freshness distinct from market reaction confirmation in daily briefs, cards, audits, quality aliases, and acquisition rows. Strategic investment/stake events now use a dedicated source pack and core-opportunity dedupe prevents supporting rows from spawning duplicate acquisition work. |
| done | Execute Event Alpha source-pack evidence acquisition | — | Event Alpha can now run bounded, fixture-first source-pack evidence plans for selected near-misses and hypotheses, validate returned source rows through deterministic identity/catalyst/impact/source-quality gates, persist `event_evidence_acquisition.jsonl`, show accepted/rejected/no-result/provider-unavailable statuses in daily briefs, cards, audits, and run ledgers, and group feedback/calibration by source pack plus accepted evidence reason codes. `make event-alpha-evidence-acquisition-smoke` proves VELVET/RUNE/ZEC accepted evidence plus context-only/no-result rows without sends/trades/paper/normal RSI rows or provider-created `TRIGGERED_FADE`. |
| done | Add Event Alpha source registry and evidence planning | — | Source Registry v2 classifies official, structured, CryptoPanic, broad-news, prediction-market, recap/SEO, derivatives, and supply sources with mission-specific confidence caps and coverage semantics. Playbook source packs and a constrained evidence planner now tell near-miss reports, daily briefs, cards, and audits what evidence would upgrade/downgrade a candidate while keeping all behavior research-only. |
| done | Make CoreOpportunity the operator-visible artifact spine | — | Visible Event Alpha core opportunities now drive research-card creation, alert-snapshot linkage, feedback readiness, artifact-doctor coverage, near-miss/upgrade reporting, and market-freshness readiness. Duplicate/route suppression blocks repeated sends only; visible core opportunities still keep cards, feedback targets, and audit targets. |
| done | Fix Event Alpha market-refresh route consistency | — | Router decisions now separate lifecycle state caps from route gates: `QUALITY_BLOCKED` rows remain store-only, but valid watchlist-level opportunities capped from requested high-priority state still route through digest/watchlist eligibility instead of `STORE_ONLY`. Artifact doctor separates quality-blocked support links that are merely present from links that are actually promoting an incident without qualified support. Targeted market refresh/near-miss ids now use canonical core-opportunity ids when available so support rows do not create duplicate refresh queue entries. |
| done | Add targeted market refresh for stale Event Alpha opportunities | — | Near-miss detection now builds an auditable targeted market-refresh queue for already-validated candidates blocked by stale/missing/unknown market context, including validated-digest rows. Refresh results persist provider/error details, before/after market context, market confirmation, opportunity score/level, and upgrade status on hypotheses, cards, audits, daily briefs, and near-miss reports. Added `market_refresh_smoke` and `make event-alpha-market-refresh-smoke` to prove VELVET/SpaceX can upgrade from stale validated digest to high priority using fresh fixture market rows while weak/no-reaction rows remain lower. |
| done | Fix Event Alpha feedback-readiness lineage | — | `catalyst_frame_e2e` research cards now carry current lineage, card path, stable feedback target/type, and useful/junk/watch commands; card indexes and daily briefs show operator group plus feedback target; artifact doctor and feedback readiness count real card Markdown files separately from `index.md`; no-alert no-send/e2e namespaces can be feedback-ready from current cards while missing lineage/targets still block readiness; opportunity audit resolves card paths and reads feedback artifacts for marked status. |
| done | Polish Event Alpha traceability and live readiness | — | Research cards now carry current run/profile/namespace lineage plus incident, hypothesis, watchlist, core opportunity, alert/snapshot/card, and source ids; legacy lineage gaps are explicit. Opportunity audit, inbox, and feedback commands now agree on core opportunity / hypothesis / incident / alert / snapshot / card / watchlist targets. Daily briefs and quality reviews show a canonical operator-view note plus market freshness readiness, profile/status output explains frame-enabled `notify_llm_quality` behavior, and new `event-alpha-feedback-readiness` plus no-send live-style frame smoke targets check handoff readiness. |
| done | Harden Event Alpha market-context freshness | — | Market confirmation now carries observed-at/source/age/freshness status through hypotheses, watchlist rows, alert snapshots, cards, audits, daily briefs, and signal-quality evals. Live-style stale, missing, or unknown-timestamp market context is capped and cannot by itself promote candidates to WATCHLIST/HIGH_PRIORITY; fixture/e2e profiles may explicitly allow stale fixture context, labeled `fixture_allowed_stale`. Fixture/example source-enrichment URLs now use provided fixture text instead of making network fetches. |
| done | Polish Event Alpha operator-facing opportunity reports | — | Daily briefs now use a core-first operator flow with lower-level row dumps under a Diagnostics Appendix, near-miss/local-only copy explains missing evidence through shared human-readable reason text, research-card links share the core/near/local/diagnostic grouping contract, inbox labels are core-opportunity-oriented, audits show each target's default presentation section, signal-quality evals assert brief section / diagnostic visibility / false-positive reason fields, and quality review only flags false positives with explicit suspicion evidence. |
| done | Harden Event Alpha card and diagnostics UX | — | Daily briefs now account for hidden diagnostic/source-noise/control support near the top, card indexes classify cards from watchlist/quality metadata with content fallback instead of relying only on filenames, and verdict-aware research-card copy avoids stale manual-review/invalidation language for validated strategic investment, proxy venue/exposure, and unknown market-dislocation cases. |
| done | Clean Event Alpha operator-facing output | — | Daily briefs now lead with executive summary plus one-shot core-opportunity sections for high-priority, validated digest, watchlist, local/capped, canonical incidents, and system health. Already-promoted opportunities are filtered out of exploratory digest/near-miss views, near-miss rows are de-duplicated by incident/asset/role/impact path, quality review only lists explicit false-positive suspicion reasons, card indexes group core/near-miss/local/diagnostic/legacy cards, and inbox wording reflects core opportunity review. |
| done | Aggregate Event Alpha core opportunities | — | Added a shared core-opportunity presentation helper so daily briefs, quality review, research-card indexes, and opportunity audit show one visible opportunity per incident/asset/role/compatible impact family while preserving supporting hypotheses, quality-capped rows, and source-noise/control rows as diagnostics. VELVET/SpaceX no longer appears simultaneously as promoted, near-miss, exploratory, and control output in default reports. |
| done | Finish Event Alpha catalyst-frame/reporting audit | — | The required Event Alpha quality/catalyst-frame workstreams and stretch near-miss/feedback readiness checks are covered by current source and fixture artifacts. Fixed the standalone `event-alpha-daily-brief` Make target so catalyst-frame fixture profiles include test artifacts and select the latest run, added `incident_id` plus source-class/incident-id calibration cohorts to feedback-only rows, and re-verified the catalyst-frame e2e and notify-style frame smoke targets. |
| done | Add Event Alpha frame-quality loop | — | Added `make event-alpha-frame-quality-loop PROFILE=catalyst_frame_e2e`, a no-send fixture-backed review chain that runs the signal-quality eval, regenerates catalyst-frame e2e artifacts, reruns quality review, incident report, impact-hypothesis report, daily brief, strict artifact doctor, and an AAVE opportunity audit. Test-artifact inclusion now applies to catalyst-frame fixture profiles so strict doctor targets inspect current fixture rows instead of skipping them. |
| done | Polish catalyst-frame live-style artifacts and operator reports | — | Added a no-send `notify_llm_quality_frame` profile and `make event-alpha-notify-llm-quality-frame-smoke` so the `notify_llm_quality` artifact shape can be proved with fixture catalyst frames, run-ledger counters, incidents, hypotheses, daily brief, quality review, cards, audit metadata, and strict doctor checks without live providers or sends. Catalyst-frame skip reasons are normalized for disabled/missing-key/budget/no-selection/deadline cases, run-ledger legacy nulls read as zero/dicts, daily briefs show the selected namespace, and quality review de-duplicates operator-facing core opportunities while preserving supporting categories and impact paths for audit. |
| done | Operationalize LLM catalyst-frame coverage and route caps | — | Live-style Event Alpha cycles now annotate rows that need catalyst-frame analysis even when the LLM provider is disabled, unavailable, skipped by prefilter/budget, or returns no analysis. Run ledgers and daily briefs expose analyzed/validated/unresolved/skipped frame counts plus skip reasons. Validated hypotheses whose ambiguous source text requires a frame are capped to exploratory/store-only when the frame is missing or unresolved, unless deterministic direct-event evidence is sufficient. Incident asset roles now demote taxonomy/search suggestions to candidate suggestions until resolver/identity validation confirms the affected asset, and compatible validated hypotheses aggregate by incident, validated asset, role, and impact-path family while preserving supporting evidence. |
| done | Prove LLM catalyst-frame validation end-to-end | — | Validated catalyst-frame rows now flow through the full Event Alpha raw-event transform before discovery-derived incidents and impact hypotheses are generated. Incidents, hypotheses, watchlist score components, cards, opportunity audits, daily briefs, and run ledgers persist main-frame role/subject/actor/object/evidence plus background/corrective/rejected-path and rule-vs-LLM resolution metadata. Added the isolated `catalyst_frame_e2e` profile and `make event-alpha-catalyst-frame-e2e-cycle`, which writes fixture artifacts under `event_fade_cache/catalyst_frame_e2e/` proving AAVE/Kraken is strategic investment, KelpDAO is rejected background/corrective context, THORChain remains exploit, MemeCore remains unknown market dislocation, ZEC remains listing, and VELVET remains proxy exposure. |
| done | Separate main catalyst from background incident context | — | Event Alpha now extracts deterministic catalyst frames from source evidence and selects an authoritative main frame before incident, impact-path, hypothesis, verdict, daily brief, card, and audit logic run. Background/historical exploit context and negated/corrective claims are persisted as support metadata and rejected impact paths, so AAVE/Kraken-style strategic-stake articles cannot be promoted as exploit/security events because of unrelated KelpDAO background or “not hacked” wording. |
| done | Enforce Event Alpha live-path quality caps and incident subjects | — | Non-hypothesis event-alert/playbook watchlist rows now go through the same quality lifecycle cap before persistence as hypothesis rows, so local-only/zero-score/insufficient-data/unknown-role rows cannot be written as active `WATCHLIST`/`HIGH_PRIORITY` states. Artifact doctor now treats path-scoped watchlist rows without embedded profile/namespace metadata as current rows for quality checks, exposing CHZ/SOL/ADA/DOGE-style conflicts. Incident subject validation now runs before canonical incident persistence so boilerplate/SEO subjects such as `About`, `All`, `LLM`, and `Polymarket Invite Code SBWIRE` are diagnostic/rejected or replaced by validated context, not valid canonical incidents. |
| done | Use final opportunity verdicts and refresh near-misses | — | Event Alpha validated-hypothesis routing now uses `opportunity_score_final` plus `opportunity_level` as the canonical route inputs; stale `opportunity_score_v2`/`hypothesis_score` are audit-only. Added near-miss detection and bounded market/enrichment refresh for validated candidates near digest/watchlist promotion, with before/after verdict diagnostics, daily brief and opportunity-audit sections, `--event-alpha-near-miss-report`, and `make event-alpha-near-miss-report`. Research-only; no notification sending, paper/live rows, normal RSI rows, trading, or provider-created `TRIGGERED_FADE`. |
| done | Require quality-qualified links for active incidents | — | Incident relevance now computes link-quality counts before deciding active/linked status. Quality-capped, local-only, insufficient-data, generic co-occurrence, unknown-role, source-noise, and sector-only links no longer promote broad external events into `active_incident`; they remain `incident_candidate` or hidden `external_context_only` rows with explicit link-quality reasons. Incident reports, daily briefs, opportunity audits, artifact doctor, and signal-quality evals expose raw/qualified/weak/quality-blocked/unknown-role link counts. |
| done | Split external-context incident relevance from raw observations | — | Incident relevance now has a distinct `external_context_only` status for broad political/sports/prediction-market/geopolitical context with no validated crypto link, while generic unlinked rows remain `raw_observation`. Live-style stores continue to hide both by default; `RSI_EVENT_INCIDENT_STORE_RAW_OBSERVATIONS=1` can store raw/external context for audits separately from `RSI_EVENT_INCIDENT_STORE_DIAGNOSTIC=1`. Incident reports, daily briefs, artifact doctor, and signal-quality evals now show diagnostic/raw/external-context counts separately. |
| done | Gate canonical incidents by crypto relevance | — | Event Alpha incident rows now carry explicit relevance statuses (`raw_observation`, `incident_candidate`, `canonical_incident`, `linked_incident`, `active_incident`, `diagnostic_only`, `rejected_incident`) plus score, reasons, warnings, and canonical persistence reason. Live-style profiles persist canonical/linked/active/candidate incidents by default while raw/diagnostic/rejected observations are hidden unless `RSI_EVENT_INCIDENT_STORE_DIAGNOSTIC=1` or fixture/debug mode is active. Daily briefs, incident reports, opportunity audits, cards, alert snapshots, watchlist metadata, artifact doctor, and signal-quality evals expose the relevance distinction so broad unlinked external events no longer look like operational incidents. |
| done | Add fresh live-style Event Alpha quality proof | — | Added `notify_llm_quality_fresh` and `make event-alpha-quality-live-smoke PROFILE=notify_llm_quality_fresh` as the clean live-style no-send proof path when stale `notify_llm_quality` artifacts are suspected. The target clears only `event_fade_cache/notify_llm_quality_fresh/`, uses wall-clock time, avoids `--event-alert-send`, writes quality/incidents/watchlist/run-ledger artifacts, and prints daily brief, quality review, incident report, and strict artifact doctor output. The fresh 2026-06-26 run had 186 watchlist rows, 18 quality-capped rows, 0 bad active watchlist rows, 155 incidents, 0 garbage canonical incident subjects, and strict doctor exit 0 with warnings only for unlinked valid incidents. |
| done | Harden quality lifecycle caps and incident subjects | — | Watchlist lifecycle caps now apply to non-hypothesis rows with bad quality fields as well as hypothesis-derived rows: local-only/exploratory, zero-score, insufficient impact/evidence/source, or `candidate_role=unknown_with_reason` rows cannot safely remain active watchlist/high-priority unless they persist an explicit non-active final state and cap reason. Stale persisted final states are recomputed when quality fields exist, and missing-quality fresh alert/playbook rows receive conservative local-only defaults. Artifact doctor now reports universal, hypothesis, non-hypothesis, capped, uncapped, legacy, diagnostic, invalid, and garbage-subject incident counts. Incident subject validation rejects boilerplate/prose/SEO fragments such as `Actions`, `Announcements`, `However`, `It`, `LLM`, `Non`, `Note`, `Only`, `Best Prediction Market Apps`, and `Polymarket Invite Code SBWIRE`; `Polymarket World Cup Volume` normalizes to `World Cup` when sports context exists. Existing garbage-subject artifacts are quarantined at read/report time and hidden unless diagnostics are explicitly requested. |
| done | Finish incident-spine quality artifacts | — | `notify_llm_quality` now has a dedicated no-send validation target (`make event-alpha-notify-llm-quality-validation-cycle`) that rebuilds its namespace, writes incidents, hypotheses, watchlist, run ledger, daily brief, incident report, quality review, and strict doctor output without requesting Telegram sends. Fresh no-incident hypothesis/watchlist/alert rows must carry `incident_link_status=no_incident` plus an explicit reason; otherwise strict doctor treats missing incident ids as artifact-health failures. Market-anomaly incident rows now preserve the validated anomaly asset as `direct_subject`, demote sector context away from direct-subject identity, and warn on `market_anomaly_missing_validated_asset`. |
| done | Make canonical incidents the Event Alpha state spine | — | Incident identity now propagates as top-level `incident_*` aliases across impact hypotheses, hypothesis-store rows, hypothesis-derived watchlist rows, alert snapshots, run-ledger rows, incident reports, and artifact doctor checks. Hypothesis watchlist keys use `incident_id + asset/sector identity + candidate_role + impact_path_type` when available, so duplicate source updates enrich the same incident/watchlist row instead of fragmenting state. Material update reasons now include incident-specific independent-source, cause-status, claim, market-reaction, causal-mechanism, and role-change reasons. Strict artifact doctor checks block fresh hypothesis/watchlist/alert rows missing required incident ids while warning on legacy rows. |
| done | Fix no-catalyst incident semantics | — | No-catalyst/no-clear-trigger/no-known-cause language is now represented as absence-of-validated-catalyst / unknown-cause metadata, not a confirmed `explains_market_move` claim. Generic words such as `No`, `Unknown`, `Market`, `Catalyst`, `Token`, and `Coin` cannot become incident primary subjects. Market-anomaly incident keys include asset identity/date/anomaly type so SOL and USDT anomalies do not merge under a generic incident; reports show `market_reaction_observed` separately from causal-mechanism confirmation. |
| done | Persist and report canonical Event Alpha incidents | — | Canonical incidents are now first-class profile-scoped artifacts under `event_fade_cache/<namespace>/event_incidents.jsonl`, with `--event-incidents-report` / `make event-incidents-report`. Incident rows link raw source ids/URLs/domains, claim history, cause status, conflicting claims, hypotheses, watchlist rows, linked assets/roles, market reaction vs causal mechanism, and incident confidence. Daily briefs, opportunity audits, research cards, alert snapshots, run-ledger rows, and watchlist material-change reasons now expose incident context. Opportunity verdicts hard-block ruled-out exploit paths and cap suspected/ecosystem incidents until causal/market evidence supports promotion. |
| done | Add Event Alpha claim semantics, incident clustering, and market-context provenance | — | Event Alpha now extracts claim polarity/cause status, clusters duplicate source rows into canonical incidents by subject/archetype/ecosystem/date, classifies candidate roles (`direct_subject`, `ecosystem_affected_asset`, proxy venue/instrument, infrastructure, macro/generic), and propagates incident/role/claim/market-context fields through impact hypotheses, watchlist rows, quality review, daily briefs, research cards, and signal-quality evals. Ruled-out or unknown-cause exploit narratives become `market_dislocation_unknown` instead of confirmed exploit paths; alleged exploits stay local-only unless confirmed; third-party ecosystem incidents can be reviewed as ecosystem-affected exposure without pretending the candidate was directly exploited. Source-enrichment cache rows are versioned by cleaner/content/text hashes so stale cleaned evidence is not reused. |
| done | Make Event Alpha quality verdicts cap watchlist lifecycle state | — | Final opportunity-quality verdicts now cap lifecycle state before active-watchlist, monitor, router, alert snapshot, daily brief, inbox, and research-card consumers see rows. `local_only`, insufficient-data, and zero-score rows cannot remain active `WATCHLIST`/`HIGH_PRIORITY`; they persist as `QUALITY_BLOCKED`/local review rows with requested/final state and a `quality_state_block_reason`. Valid `watchlist`/`high_priority` verdicts still allow active watchlist states, and `EVENT_PASSED`/`ARMED` lifecycle progression remains possible for validated watchlist-quality rows. Daily briefs and quality review show `Quality-Capped Watchlist Rows` separately; artifact doctor strict mode blocks fresh uncapped state conflicts and warns on legacy conflicts. Block reasons now describe missing evidence rather than positive evidence, and candidate-discovery funnel metrics distinguish raw terms from candidate-like/accepted/context-validated/promoted candidates. `TRIGGERED_FADE_RESEARCH` remains preserved only for deterministic `event_fade.py` + `proxy_fade`. |
| done | Make Event Alpha routing obey final quality verdicts | — | Router decisions now apply a final opportunity-quality gate before operator-facing research routes. `local_only`, `exploratory`, insufficient-data/source-noise/ticker-collision, and zero-score verdicts are downgraded to local/store-only output even if the older watchlist/playbook path requested digest/watchlist/high-priority routing. Alert snapshots now persist canonical requested route/tier, final route/tier after quality gate, alertable-after-quality-gate, block reason, and snapshot quality classification (`current_clean`, `quality_gated_local`, `legacy_conflict`, `missing_final_route`, `stale_pre_quality_gate`), while preserving requested/legacy route values for audit. Notification plans, routed Telegram copy, inbox queues, daily briefs, cards, quality review, policy simulator, and artifact doctor use the final route by default. Quality-gated local-only rows and legacy conflicts are separated from would-send/delivered review queues; fresh conflicts or missing final routes block strict doctor checks, with strict-legacy available for migration audits. Candidate-discovery funnel and quality-tuning reports now distinguish raw terms, resolver attempts, accepted/rejected candidates, context validation, promotion, near-threshold candidates, weak patterns, and blocker sources. `TRIGGERED_FADE_RESEARCH` remains preserved only for deterministic `event_fade.py` + `proxy_fade`. |
| done | Add fresh Event Alpha quality coverage profile | — | Added `notify_llm_quality`, a live-style LLM quality-validation profile that writes under `event_fade_cache/notify_llm_quality/` and has a dedicated no-send scheduled target (`make event-alpha-notify-llm-quality-scheduled`) using the real wall clock, run lock, and profile namespace without passing `--event-alert-send`. Added `--event-alpha-quality-coverage-report` / `make event-alpha-quality-coverage-report` to read raw latest-run hypothesis/watchlist/alert-snapshot rows and block fresh rows missing canonical top-level signal-quality fields. Quality review and impact-hypothesis reports now warn when a namespace likely contains pre-quality-layer artifacts while `quality_validation` rows are clean. |
| done | Canonicalize Event Alpha top-level quality artifacts | — | Fresh hypothesis, watchlist, and alert snapshot rows now receive canonical top-level signal-quality fields even when context is missing: conservative local-only/insufficient-data defaults, explicit `why_local_only`/`why_not_watchlist`, manual verification items, and persisted upgrade/downgrade diagnostics. Artifact doctor strict mode now checks top-level fields directly and reports fresh hypothesis/watchlist/alert missing counts separately from legacy rows, so nested `score_components` can no longer mask bad fresh artifacts. Quality review/audit/policy simulation now show quality source/coverage, named policy scenarios, and richer candidate-discovery funnel diagnostics. Signal-quality fixtures expanded to 21 cases including political meme proxy, CryptoPanic tag, URL-only false positive, candidate-discovery, and Hyperliquid/HYPE identity cases. |
| done | Validate signal-quality layer + persist upgrade/downgrade paths | — | Validated the signal-quality layer against fresh fixture artifacts in an isolated `quality_validation` namespace: hypothesis, hypothesis-derived watchlist, and alert-snapshot rows already carry the enforced quality fields (doctor `missing_total=0`), and router/verdict pass the 16/16 signal-quality eval. Patched the one gap — `upgrade_requirements`/`downgrade_warnings` are now persisted on hypothesis rows (via the existing `explain_upgrade_path`) instead of only computed on-demand. Added a `quality_validation` fixture profile and `make event-alpha-quality-validation-cycle` for reproducible no-send validation. Research-only; no trading, paper trades, normal RSI rows, or LLM/provider-created `TRIGGERED_FADE`. |
| done | Operationalize Event Alpha signal-quality loop | — | New hypothesis/watchlist/alert snapshot artifacts now enforce required quality fields, and artifact doctor reports missing quality metadata with strict-mode escalation. Added `--event-alpha-quality-review`, `--event-alpha-policy-simulate`, `--event-alpha-export-signal-quality-cases`, and the no-send `make event-alpha-quality-loop` chain. Daily briefs and research cards now surface signal-quality summaries, opportunity distributions, candidate discovery funnel counts, and upgrade/downgrade context. The export writes proposed benchmark cases only and never mutates canonical fixtures. |
| done | Add Event Alpha signal-quality workbench | — | Added a fixture-backed signal-quality benchmark, candidate-level opportunity audit report, upgrade/downgrade explanations, feedback/calibration cohorts by impact path/candidate role/evidence/market/opportunity levels, and notification quality modes. `make event-alpha-signal-quality-eval` checks VELVET/RUNE/ZEC/CHZ positive cases, weak BTC policy/CFTC co-occurrence, market-anomaly controls, listing/unlock cases, and source-noise/word-collision false positives. `make event-opportunity-audit TARGET=... PROFILE=notify_llm` explains an individual candidate's evidence chain, missing evidence, router state, upgrade path, and downgrade risks. Notification profiles now default to `validated_digest` visibility; exploratory output is deliberate. Research-only; no normal RSI rows, paper/live writes, trading, or LLM/provider-created `TRIGGERED_FADE`. |
| done | Add market/evidence opportunity verdict layer | — | Event Alpha validated hypotheses now get first-class market confirmation, evidence/source quality, and final opportunity verdict metadata. New pure scorers classify market confirmation (`none/weak/moderate/strong`), source class/evidence specificity, and final opportunity levels (`local_only`, `exploratory`, `validated_digest`, `watchlist`, `high_priority`). Validated hypothesis routing now blocks local/exploratory verdicts, lets digest/watchlist verdicts into capped research digests, and can route high-priority verdicts through the research escalation lane without changing `event_fade.py` or `TRIGGERED_FADE`. Watchlist rows, snapshots, daily briefs, reports, cards, and inbox grouping expose the new fields. |
| done | Add impact-path validator and v2 digest gate | — | Event Impact Hypotheses now carry pure impact-path validation metadata: `impact_path_type`, `candidate_role`, `impact_path_strength`, `evidence_specificity_score`, `digest_eligible_by_impact_path`, and `opportunity_score_v2`. Validated-hypothesis digest routing now requires the v2 opportunity score threshold and blocks generic co-occurrence by default, while allowing strong paths and medium paths with market confirmation. RUNE/ZEC/CHZ/VELVET-style source evidence can stay digest-eligible; BTC quantum/CFTC/policy-style weak co-occurrence remains local-only. Watchlist rows, alert snapshots, daily briefs, cards, hypothesis store reports, and inbox grouping expose the new metadata. Still research-only; no normal RSI rows, paper/live writes, trading, or LLM/provider-created `TRIGGERED_FADE`. |
| done | Promote validated impact hypotheses safely | — | Event Impact Hypothesis watchlist rows now use the validated token identity rather than `candidate_symbols[0]`, warn on candidate-order mismatches, and fall back to `SECTOR` when validation lacks a real asset. Validated token-level `RADAR` hypotheses can enter a capped daily research digest only after the quality gate passes: validated identity, `impact_path_validated` stage by default, no source-noise/ticker collision, score threshold, non-ambiguous playbook, and known external catalyst or explicit direct token-event evidence. Weak catalyst/token co-occurrence stays local-only with `impact_path_not_validated`/`weak_validated_local_only` diagnostics unless explicitly allowed. Delivered digest route decisions now write Event Alpha alert snapshots with plain `symbol`/`coin_id` plus validated identity fields so the notification inbox can request useful/junk feedback. Bounded candidate-discovery searches can suggest missing crypto assets in LLM profiles, but deterministic identity/catalyst validation is still required. Hypotheses still cannot create `WATCHLIST`, `HIGH_PRIORITY`, paper/live rows, normal RSI rows, trading, or `TRIGGERED_FADE`. Reports default to latest-run rows with `ALL_HISTORY=1` for historical review. |
| done | Make LLM notification profiles send on every clean run | — | A fresh `notify_llm` run after parallel LLM work completed but sent nothing because the exploratory digest lane was still on a 24h cooldown. `notify_llm` and `notify_llm_deep` now mirror `notify_no_key` per-run delivery behavior: heartbeat/digest/exploratory cooldowns are zero, content dedupe is disabled in profile defaults and scheduled Make targets, while run locks and in-flight safety remain active. This changes delivery frequency only; no scoring, alertability, normal RSI rows, paper/live writes, trading, or `TRIGGERED_FADE` eligibility changed. |
| done | Parallelize Event Alpha LLM notification calls | — | OpenAI-backed Event Alpha LLM extraction and relationship analysis now run uncached provider calls through a bounded thread pool while preserving cache hits, budget/deadline guards, and deterministic report rows. `notify_llm` now defaults to 12 parallel calls, 30s LLM timeouts, and a 600s notification runtime cap; `notify_llm_deep` uses 16 parallel calls and 45s LLM timeouts. This is runtime/coverage only and does not change scoring, routing safety, normal RSI rows, paper/live writes, trading, or `TRIGGERED_FADE` eligibility. |
| done | Guard OpenAI LLM loops with notification runtime deadlines | — | Full `notify_llm` testing showed the GDELT fetch cap was fixed, but slow OpenAI extraction calls could still consume the notification cycle after entering the LLM loop. Raw-event extraction and relationship analysis now accept a runtime deadline, notification runs pass the cycle deadline into both configs, cache hits remain usable, and uncached provider calls skip with `skipped_runtime` once the deadline is exhausted. |
| done | Cap live GDELT catalyst-search retries | — | Live notification runs exposed GDELT catalyst search repeatedly fetching once per hypothesis/query after 429/timeouts. GDELT catalyst search now performs one live fetch per provider search, propagates provider warnings into health/backoff, and then local filtering/backoff handles the rest. The fresh `notify_no_key` scheduled run completed in ~56s and delivered the exploratory digest plus health heartbeat. |
| done | Raise Event Alpha LLM budget controls | — | OpenAI-backed Event Alpha profiles now have much higher checked-in LLM caps, and local `RSI_EVENT_LLM_*` budget env vars can override profile defaults for relationship candidates, raw-event extraction events, run/day call caps, estimated daily cost, estimated per-call cost, and cache TTL. Local `.env` on the owner machine is set to 200 relationship candidates, 300 extraction events, 200 calls/run, 1000 calls/day, a $25/day estimated cap, and 24h cache TTL. This only changes LLM depth; it does not change alert scoring, send guards, normal RSI rows, paper/live writes, trading, or `TRIGGERED_FADE` eligibility. |
| done | Make Event Impact Hypothesis artifacts latest-run diagnosable | — | `event_impact_hypotheses.jsonl` reports can now filter by latest run, explicit run id, or since timestamp, while summarizing total/latest/historical/legacy row counts and schema gaps. Stored hypotheses carry generated vs executed query details, rejected validation samples include `result_score`, daily briefs show rejected evidence reason/title samples, reports audit suspicious external entities parsed as crypto candidates, and `event-alpha-notify-llm-deep-scheduled` runs the bounded `notify_llm_deep` profile under the same guarded notification path. This is reporting/observability only and does not change promotion, routing, normal RSI rows, paper/live writes, trading, or `TRIGGERED_FADE` eligibility. |
| done | Make notify_no_key send on every clean run | — | The `notify_no_key` profile now sets heartbeat, exploratory, daily, and instant lane cooldowns to zero and disables stable content dedupe so every clean no-key notification run sends operator-visible Telegram output. Run lock and in-flight safety remain active, and this is delivery frequency only: no alert scoring, normal RSI routing, paper/live writes, trading, or `TRIGGERED_FADE` eligibility changed. |
| done | Clean Event Impact Hypothesis semantics | — | Impact hypotheses now separate external entities (OpenAI/SpaceX/etc.) from crypto candidate assets and rejected candidate assets, carry `hypothesis_score`, score components, typed validation stages, typed search query details, capped rejected validation evidence samples, and `why_not_promoted` diagnostics. Sector hypotheses can generate candidate-discovery queries without named tokens; discovery hits may suggest candidates, but watchlist promotion to token-level `RADAR` now requires catalyst-linked deterministic identity validation (`catalyst_link_validated`, `market_confirmed`, or `promoted_to_radar`). Reports/daily briefs expose schema-audit, stage/score/entity/candidate/rejection/why-not-promoted summaries, and source enrichment strips ticker/menu/footer/price-table noise before LLM/resolver context while preserving legitimate HYPE/Hyperliquid text. |
| done | Harden Event Alpha notification provider failure handling | — | Fresh `notify_llm` run diagnostics showed GDELT 429 rate limiting, RSS feed-level 403s, and live OpenAI call runtime risk. GDELT remains a real provider backoff condition; multi-feed RSS now keeps per-feed `feed_failure` warnings visible without tripping whole-provider backoff when another feed returns usable rows. OpenAI relationship/extraction calls now use explicit timeout config (`RSI_EVENT_LLM_OPENAI_TIMEOUT`, `RSI_EVENT_LLM_EXTRACTOR_OPENAI_TIMEOUT`) with conservative caps in OpenAI-backed Event Alpha profiles. |
| done | Persist and inspect Event Impact Hypotheses | — | Impact hypotheses now append profile/run/namespace-scoped JSONL rows under the Event Alpha artifact namespace, with candidate provenance (`taxonomy`, `llm_extraction`, `deterministic_resolver`), suggested vs validated assets, flattened validated symbol/coin-id fields, promoted watchlist keys, validation/search status, query counts, and rejection reasons. Added `--event-impact-hypotheses-report`, `--event-impact-hypotheses-inbox`, `make event-impact-hypotheses-report`, `make event-impact-hypotheses-inbox`, and `make event-impact-hypothesis-smoke`; run ledgers and daily briefs now surface separate hypothesis-search skip reasons and stored hypothesis row summaries. `notify_llm` gets bounded full-source enrichment for LLM context, `notify_no_key` remains no-key/no-full-source enrichment, and `notify_llm_deep` is available as a higher-budget opt-in profile. |
| done | Harden Event Alpha impact discovery | — | Impact hypothesis matching now uses boundary/phrase-aware category rules instead of substring checks, with tighter sports/political/prediction-infra/stablecoin/pre-IPO context requirements. Hypotheses now carry `hypothesis_scope`; unvalidated sector/venue/infra hypotheses are stored as `SECTOR` watchlist rows with candidate assets as metadata, and only identity-validated evidence narrows them to token-level `RADAR` rows. A separate impact-hypothesis validation search executes through the catalyst-search provider layer and reports its own query/result metrics. Source enrichment now runs before LLM raw extraction in the unified cycle. `notify_no_key`/`notify_llm` enable hypothesis validation search; `notify_llm` explicitly includes optional CryptoPanic evidence while `notify_no_key` remains no-key. |
| done | Harden Event Alpha source/search observability | — | RSS live intake now treats one-feed HTTP failures as per-feed `feed_failure` warnings and continues remaining feeds, while configured fail-fast is reserved for provider/network-level failures. Catalyst search now records skip-reason counters (`provider_unavailable`, `provider_backoff`, `no_anomalies_over_threshold`, `anomaly_identity_missing`, `runtime_budget_exhausted`, `query_limit_zero`, etc.) in pipeline reports, run ledger rows/reports, and daily briefs. `notify_llm` includes CryptoPanic in catalyst-search providers when configured; `notify_no_key` remains no-key. |
| done | Add Event Alpha impact hypothesis layer | — | Added deterministic `EventImpactHypothesis` generation for external catalysts, seed sector/asset taxonomy, identity-safe validation, hypothesis-driven search queries, optional cached source enrichment, `HYPOTHESIS` watchlist state, pipeline/run-ledger/daily-brief counters, and exploratory digest copy for hypotheses. Plain hypotheses are store-only/exploratory and not alertable; validated evidence may promote a hypothesis row to `RADAR`, but `TRIGGERED_FADE` still only comes from `event_fade.py` + `proxy_fade`. |
| done | Add Event Alpha exploratory notification digest | — | Added a separate `exploratory_digest` lane for top suppressed/store-only/raw-evidence rows during notification burn-in. It is enabled by default only in `notify_no_key`/`notify_llm`, has its own cooldown/dedupe state, excludes source-noise/ticker-collision controls unless explicitly opted in, appears separately in notification runs/inbox/daily brief, and cannot make rows alertable or create `TRIGGERED_FADE`. Added a guarded Telegram recipient diagnostic that redacts chat IDs and refuses without send guard/Telegram config. CryptoPanic remains an optional recommended source, not a blocker for notification profiles. |
| done | Polish Event Alpha notification SLO semantics | — | SLO status now separates would-send previews, send-guard/config blocks, and actual sender-side delivery failures. Preview runs no longer count as alertable delivery failures; send-requested runs without `RSI_EVENT_ALERTS_ENABLED` classify as `NO_SEND_CONFIG`; guard-enabled delivery failures remain `BLOCKED`. Latest meaningful send status controls the headline so older config-blocked bootstrap rows cannot mask newer delivered runs. Still research-only; no live trading, paper trades, normal RSI signal writes, or LLM-created `TRIGGERED_FADE`. |
| done | Polish Event Alpha scheduled notification operations | — | Added a profile-aware environment doctor, namespace-scoped emergency notification pause/resume switch, scheduler status report plus dry-run launchd template generation, notification SLO/freshness report, and redacted notification artifact pack export. Paused runs still write research artifacts and blocked delivery rows with `notifications_paused`; scheduled sends remain guarded by Telegram config, `RSI_EVENT_ALERTS_ENABLED=1`, run locks, delivery dedupe, cooldowns, and research-only copy. No live trading, paper trades, normal RSI signal writes, or LLM-created `TRIGGERED_FADE`. |
| done | Polish Event Alpha day-1 notification delivery fidelity | — | Added structured Telegram delivery results, `partial_delivered` delivery state, stable dedupe keys separate from exact content hashes, configurable `RSI_EVENT_ALPHA_NOTIFICATION_PARTIAL_MARKS_COOLDOWN`, blocked/partial/would-send reporting polish, partial-delivery inbox routing, and go/no-go operator commands for provider health/reset, delivery reports, and inbox review. Event Alpha now uses the structured sender directly while old `send_telegram()` bool callers remain compatible. Still research-only; no live trading, paper trades, normal RSI signal writes, or LLM-created `TRIGGERED_FADE`. |
| done | Polish Event Alpha day-1 notification delivery reliability | — | Added structured Event Alpha notification send-attempt metadata, recipient/chunk counts in the delivery ledger, partial-delivery handling, recent in-flight content dedupe with configurable `RSI_EVENT_ALPHA_NOTIFICATION_IN_FLIGHT_GRACE_MINUTES`, fixture smoke delivery-ledger coverage, a new `--event-alpha-notify-go-no-go` / `make event-alpha-notify-go-no-go` readiness report, and inbox/run/delivery report separation for delivered, failed, duplicate, in-flight, blocked, and would-send states. Still research-only; no live trading, paper trades, normal RSI signal writes, or LLM-created `TRIGGERED_FADE`. |
| done | Harden Event Alpha day-1 notifications for scheduled use | — | Added a per-profile notification run lock (`event_alpha_run_lock.py`; fresh→skip+record, stale/dead-PID→recover, released on completion) and an idempotent delivery ledger (`event_alpha_notification_delivery.py`; planned/sending/delivered/failed/skipped_duplicate/blocked with content-hash dedupe). Cooldown is only marked after a real delivery. Lock/delivery outcome is hung off Codex's notification-runs ledger and surfaced in the notification-runs report, daily brief, and artifact doctor (warns on failed deliveries). New `--event-alpha-notification-deliveries-report` and `--event-alpha-notification-retry-failed` (dry-run scaffold; resend is a TODO), plus `event-alpha-notify-no-key-scheduled`/`event-alpha-notify-llm-scheduled` Make targets. Still research-only: sends require `--event-alert-send` + `RSI_EVENT_ALERTS_ENABLED=1`; `TRIGGERED_FADE` only from `event_fade.py` + `proxy_fade`; no trading/paper/RSI rows. |
| done | Polish Day-1 Event Alpha notification provider ergonomics | — | Added profile-scoped provider health report/reset commands, one-shot notification backoff ignore that preserves health state on forced success, actual-card counting that excludes `index.md`, no-send `event-alpha-day1-start` startup targets, and post-run next-step guidance for inbox/brief/doctor/provider reset/feedback. Everything remains research-only; no live trading, paper trades, normal RSI signal writes, live-provider smoke, or LLM-created `TRIGGERED_FADE`. |
| done | Polish Day-1 Event Alpha notification operations | — | Profile/namespace-aware report commands now load notification runs, run ledgers, alert snapshots, feedback rows, and research-card writes from the requested profile unless an explicit path override is supplied. Added a notification inbox for unreviewed sent/would-send/card/degraded items, a fixture fake-sender notification smoke, notification-run UX summaries, and daily brief fixed-clock warnings. Everything remains research-only; no live trading, paper trades, normal RSI signal writes, live providers in smoke, or LLM-created `TRIGGERED_FADE`. |
| done | Make Event Alpha notification clocks production-safe | — | Split fixture determinism from operational notification clocks: fixture Make targets use `EVENT_FIXTURE_NOW`, while profiled/notification/send/burn-in targets use wall-clock UTC unless `EVENT_RESEARCH_NOW` is explicitly set. Status, preflight, notification checklist/preview, daily brief, and run-ledger rows disclose clock mode/age, and stale/future fixed clocks block actual notification sends unless explicitly overridden. Everything remains research-only; no live trading, paper trades, normal RSI signal writes, or LLM-created `TRIGGERED_FADE`. |
| done | Harden Event Alpha notification runtime reliability | — | Live CoinGecko market enrichment now fails soft in notification mode, records `coingecko:market_enrichment` health/backoff, and continues discovery/anomaly work with empty market rows. Notification cycles have a last-resort degraded wrapper that writes run and notification ledgers with heartbeat would-send accounting on unexpected pipeline errors. Preview/checklist/report output now surfaces partial/degraded state, provider backoff, runtime/timeout/fail-fast settings, and preview-vs-send readiness. Everything remains research-only; no live trading, paper trades, normal RSI signal writes, or LLM-created `TRIGGERED_FADE`. |
| done | Harden day-1 Event Alpha notifications | — | Added namespace-scoped notification cooldown/count/dedupe keys, notification startup checklist, notification-run summary artifact/report, conservative notification runtime/provider fail-fast knobs, day-1 readiness semantics that do not require prior burn-in rows, exact DAY-1 UNVALIDATED/Trading action NONE message copy, and startup Make targets. Actual delivery still requires `--event-alert-send`, `RSI_EVENT_ALERTS_ENABLED=1`, and Telegram config. Everything remains research-only; no live trading, paper trades, normal RSI signal writes, or LLM-created `TRIGGERED_FADE`. |
| done | Add day-1 Event Alpha notification burn-in | — | Added `notify_no_key` and `notify_llm` profiles, `notification_burn_in` artifact/run metadata, lane-specific send state for daily digest, instant escalation, triggered fade, and health heartbeat, guarded notify cycle/preview/test-send commands, research-card-aware Telegram copy, and day-1 readiness language. Actual delivery still requires `--event-alert-send` plus `RSI_EVENT_ALERTS_ENABLED=1` and Telegram config; dry runs print would-send counts. Trading, paper trades, normal RSI rows, and LLM-created `TRIGGERED_FADE` remain out of scope. |
| done | Make Event Alpha artifacts profile-clean | — | Added `event_alpha_artifacts.py` for profile/run-mode/namespace path context, `event_alpha_artifact_doctor.py` for artifact lineage checks, run-ledger and alert-snapshot lineage fields, test/fixture/replay and legacy/default filtering in burn-in/readiness/health/brief/explain checks, snapshot availability classification, strict doctor mode, manifest/doctor data in burn-in packs, and artifact lineage in research cards. Profiled Make targets and artifact-backed reports now resolve namespaced paths from the requested profile before loading rows, disclose profile/namespace/run-mode/run-ledger/alert-store context, and `--event-alpha-preflight` checks profile paths, provider readiness, LLM budget/key state, and send guards before daily burn-in. Legacy rows still require an explicit migration flag. Everything remains research-only and cannot send, trade, paper trade, write normal RSI signal rows, or create `TRIGGERED_FADE`. |
| done | Add Event Alpha v1 operations gates | — | Added explicit v1 readiness flags, freshness health guard, weekly tuning worksheet, clean burn-in review-pack export, lifecycle timelines in research cards, and disabled launchd/cron schedule templates. All remain artifact-only research surfaces and cannot send, trade, paper trade, write live signal rows, or create `TRIGGERED_FADE`. |
| waiting | Let paper scoreboard accrue live data | system | Current book is 75 closed / 19 open. Actionable has 32 trades at 56% wins, -0.29% average, +0.37% trimmed mean, and +2.44% median; control has 43 trades at 33% wins, -6.37% average, -5.08% trimmed mean, and -5.24% median. Seven absolute-50% outliers are provider-consistent and remain in all statistics. The separation is encouraging but still comes from one downtrend regime with small state cohorts, so keep waiting before changing live priors or risk rules. |
| waiting | Validate edge-prior conviction buckets live | system | 2026-07-10 `main.py --report` has 192 matured 7d observations with conviction buckets low 15 / med 150 / high 27 and confirm rates 27% / 58% / 63%. The ordering is encouraging, but the high bucket remains small and the paper book is still one broad downtrend regime. Keep waiting before touching `RSI_REGISTRY_PRIORS` or live priors. |
| waiting | Observe live state cohorts | system | 2026-07-10 `--score --cohorts` has state metadata on 69 of 75 closed paper trades, but it does not yet separate actionable state cohorts and the actionable book is only 32 trades from one broad regime. Keep waiting before any state-conditioned live rule. |
| waiting | Confirm state-slice candidate cohorts | system | Volume-PIT 5y research replicated several candidates (breakdown_risk crisis-vol −19; mean_reversion washout +14 / risk_on −8). The 2026-07-10 live report now has state metadata on 91 matured 7d rows, but multiple buckets remain tiny and there is no stable setup×state live cross-check yet. |
| todo | Review volume-PIT registry prior export | human | `research/registry_priors_volpit_2026-06-10.json` is the first full-cycle, survivorship-reduced calibration artifact. Review before any `RSI_REGISTRY_PRIORS` opt-in. |
| todo | Build event-fade validation sample | open | Event-fade engine plus fixture event radar, clean CoinGecko-universe bridge, neutral runtime alias default that keeps fixture aliases out of real-source review cycles, fixture-backed Binance/Bybit announcement parsers, captured official Binance CMS WebSocket payload parsing, opt-in live Binance/Bybit announcement fetches, raw Binance announcement cache listening, research-only JSONL cache refresh with redacted run diagnostics and `--event-discovery-runs` reporting, cached-snapshot validation export, redacted `--event-discovery-status` provider readiness checks, structured calendar/unlock parsers, CryptoPanic fixture parser plus opt-in live posts fetch, GDELT fixture parser plus opt-in live Article List fetch, project-blog/RSS fixture parser plus opt-in live RSS/Atom fetch, newline RSS URL-file support, no-key `make event-fade-public-rss-review-cycle` with expanded targeted proxy RSS searches, no-key `make event-fade-gdelt-review-cycle` for GDELT Article List proxy-news rows, no-key `make event-fade-polymarket-review-cycle` for Polymarket Gamma dated catalyst/control rows, no-key `make event-fade-no-key-review-cycle` to aggregate RSS, GDELT, and Polymarket into one source-summarized bundle, conservative source-text date inference plus rule-based external-asset extraction for explicit IPO/exposure/prediction-market/sports-match phrasing with event-time source/confidence provenance for RSS/news rows, 30-day lookback, no-event-time `proxy_attention` review rows, asset-role classification (`proxy_instrument`/`proxy_venue` vs `mentioned_asset`/`infrastructure`/`ticker_word_collision`) with proxy venues review-only by default, explicit low-classifier/low-event-time `NO_TRADE` forcing, canonical catalyst dedupe, merged point-in-time-safe enrichment payloads across deduped raw sources, stricter publisher/source-origin/common-word resolver guards, market-recap confidence penalties, `--event-alert-report` research-alert ranking into store-only/radar/watchlist/high-priority/triggered-fade tiers, and generic identity-word resolver guards including the live Polymarket BILL/bill false-positive guard, external IPO/sports/prediction-market catalyst parsers, Coinalyze-style derivatives enrichment plus opt-in live Coinalyze REST snapshots with optional `future-markets` symbol auto-resolution, Tokenomist/Etherscan/Arkham/Dune-style supply/on-chain enrichment, grouped `--event-fade-auto-report`, `--event-fade-export-sample`/`--event-fade-export-cache-sample` JSONL/CSV exports with raw/min/max source timestamps, research-cache transition timestamps, evidence-fingerprint-safe `--event-fade-merge-sample` review-status/label/outcome preservation, `--event-fade-export-outcome-prices` local price-fixture generation with 1d/1h interval support, `--event-fade-fill-outcomes` trigger/baseline outcome filling with price interval/source metadata and review-only human event-time baselines, `--event-fade-labeling-queue` prioritization with event-time source/confidence and source-origin surfacing plus low-confidence trigger time confirmation and proxy event-time confirmation, `--event-fade-review-packet` Markdown review packets with source-origin context, compact evidence-checked `--event-fade-export-review-template`/`--event-fade-check-review-template`/`--event-fade-apply-review-template` sidecar label workflow with derived source-origin columns and separate review provenance plus human event-time confirmation fields, `--event-fade-review-bundle`/`--event-fade-cache-review-bundle` manifested local workspace exports with sample-quality, review-gate, per-source provider summaries, per-source origin/publisher summaries, bundle-local `review_guide.md` with provenance rules, and optional bundle-local price export/outcome fill, prior-reviewed-sample merging, and empty-cache warnings, fixture-backed `make event-fade-review-cycle`, configured-source `make event-fade-configured-review-cycle`, and `--event-fade-review-sample` coverage/metrics/cohort/baseline/latency/diversity/source-leakage blocker plus asset-role/event-time-source/source-provider/source-origin cohorts, low-confidence trigger event-time blocker, proxy source-provider diversity gate, and next-sample-work report exist. Review evidence now requires both `review_status=reviewed` and a known `human_label`, while promotion remains blocked until reviewed rows also have `reviewed_by` and `reviewed_at`; research alerts do not require labels but remain investigation prompts, not trade signals; `proxy_attention` rows without event times are useful sample leads but remain `NO_TRADE`, and `proxy_venue` rows stay digest/watchlist-review-only unless explicitly opted in after evidence. Current local status shows no always-on configured event sources ready. The 2026-06-17 no-key refresh at `/tmp/event_fade_no_key_review_bundle_20260617_continue` produced 120 rows from RSS/Polymarket, with 24 proxy candidates, 20 proxy-context controls, 8 direct rows, 68 ambiguous rows, 116 missing machine event times, 0 eligible rows, and 0 triggers; GDELT was rate-limited with HTTP 429 and produced a zero-row diagnostic. The next step is human-labeling the balanced 74-row sidecar, confirming event times in the `human_event_time*` fields, and checking whether reviewed proxy-instrument candidates justify better automated event-time extraction before filling outcomes or considering any live routing/paper tracking. |
| todo | Label latest event-fade no-key bundle | human | Latest balanced bundle: `/tmp/event_fade_no_key_review_bundle_20260617_continue`. It has 120 rows: 24 proxy candidates, 20 proxy-context controls, 8 direct rows, 68 ambiguous rows, 0 triggers, and 116 missing machine event times. Start with `review_guide.md`, read `review_packet_balanced.md`, then fill the diversity-first `review_template_balanced.csv` for 74 gate-coverage rows: 24 proxy candidates and 50 controls. The proxy slice prioritizes all 14 available `proxy_instrument` rows before filling the remaining 10 slots with `proxy_venue` review-only rows, and the sidecar surfaces `external_asset` plus `source_providers` to make catalyst identity and provider diversity reviewable. Set `review_status`, `reviewed_by`, `reviewed_at`, `human_label`, and any confirmed `human_event_time*` fields; use `external_asset`, `primary_source_url`, `source_search_url`, `source_date_hint`, `source_providers`, `primary_raw_title`, `review_prompt`, and `event_time_review_hint` only as reviewer aids. Before applying the sidecar, run `main.py --event-fade-check-review-template SAMPLE TEMPLATE` to catch changed evidence, missing provenance/labels/outcomes, and valid proxy labels without explicit catalyst timing. |
| done | Extend PIT history depth beyond demo limits | — | Solved without a Pro key: `backtest --pit-volume` ranks the full Binance USDT pool by trailing 30d dollar volume per date (5y, point-in-time, cached). Details in `DEVLOG.md` 2026-06-10. |
| done | Confirm bull/chop state cohorts | — | Volume-PIT 5y run covered BULL 60.9k / CHOP 23.8k / BEAR 46.7k base-days: gating-map directions all confirmed (mean_reversion CHOP +10 n=800; dip_buy/trend_continuation BULL positive-thin; breakdown_risk no edge). See `research/VOLUME_PIT_BACKTEST_2026-06-10.md`. |
| done | Walk-forward check of the CHOP mean_reversion edge | — | Added setup×BTC-market walk-forward output and reran `.venv/bin/python -m crypto_rsi_scanner.backtest --pit-volume --top-n 100 --days 1825 --walk-forward`. `mean_reversion`×`CHOP` test-fold edges were +2, +25, +26, so the +10 aggregate was not a one-episode artifact. See `research/VOLUME_PIT_WALK_FORWARD_2026-06-17.md`. |

## Next

| status | item | owner | notes |
|---|---|---|---|
| done | Make Event Alpha burn-in readiness auditable | — | Daily brief and explain-last-run now select the latest row for the requested `--event-alpha-profile` and show requested/selected/profile-match status. Burn-in scorecards include artifact coverage diagnostics for alert snapshots, feedback, outcomes, missed rows, provider health, and LLM budget. `--event-alpha-burn-in-checklist` / `make event-alpha-burn-in-checklist` reports `READY_FOR_RESEARCH_SEND`, blockers, warnings, and next actions. Replay policy comparison now emits candidate-level score/tier/route diffs with optional feedback/outcome context, profile reports expose artifact-policy contracts, provider wrappers accept deterministic `now`, and research cards include latest watchlist monitor updates. |
| done | Polish Event Alpha burn-in daily radar | — | Profile-aware daily brief/explain/router/cards now apply or infer Event Alpha profiles, provider health rows use service/role keys with legacy fallback, active watchlist monitor can consume derivative/supply enrichment hints without creating triggers, raw replay can compare baseline/LLM/prior/router/profile policies, routed decisions expose stable `alert_id`/`card_id` values for cards and feedback, research cards can auto-write selected tiers, and `--event-alpha-burn-in-scorecard` summarizes recent runs/alerts/feedback/missed/provider/LLM budget health. All paths remain research-only and cannot create `TRIGGERED_FADE`, paper trades, live DB signal rows, or execution. |
| done | Exclude EURC from the RSI universe | Codex | The 2026-07-10 audit exposed EURC (`euro-coin`) as a kept rank-111 stablecoin. Live and backtest hygiene now exclude the exact `eurc` symbol without broad euro-name matching; the refreshed 200-row audit has EURC excluded as `stable_like`, 0 EURC kept, and 58 total exclusions. |
| todo | Use LLM extraction/advisory analyzer for event-review QA | human/open | `main.py --event-llm-extract-report` and `make event-llm-extract-eval` run fixture/OpenAI raw-event extraction for catalysts, crypto asset mentions, source-noise terms, and date hints. `shadow` mode is report-only; `advisory` mode may append quote-validated resolver hints before deterministic discovery, but extracted assets still require deterministic resolver validation. `main.py --event-llm-shadow-report`, `make event-llm-eval`, and `main.py --event-alert-report --with-llm` compare rule classifications with relationship analysis and can run opt-in advisory tier/playbook quality control when `RSI_EVENT_LLM_MODE=advisory`. Use these to reduce source-noise/ticker-collision junk and find missed proxy asset mentions while building the reviewed event-fade sample. They still cannot create `TRIGGERED_FADE`, affect normal RSI alerts, paper trade, write live signal rows, or change event-fade eligibility. |
| done | Integrate LLM raw extraction upstream in Event Alpha cycle | — | `main.py --event-alpha-cycle --with-llm` now runs quote-validated raw-event extraction before deterministic discovery resolution, appends high-confidence extraction hints to raw evidence, and then reruns normal resolver/classifier/event-alert/watchlist/router logic. Extracted assets remain resolver hints only and cannot create candidates, alerts, paper trades, live DB rows, or `TRIGGERED_FADE` without deterministic validation. |
| done | Add deterministic Event Alpha research clock | — | `RSI_EVENT_RESEARCH_NOW` and `--event-now` now pin event discovery/alert/LLM/watchlist/fade fixture and review-artifact paths, and fixture Make targets default to `2026-06-15T16:00:00Z`. This completes the Pro-model Phase 0 time-determinism pass; normal RSI scans still use wall-clock time. |
| done | Add unified Event Alpha cycle | — | `main.py --event-alpha-cycle` plus `make event-alpha-cycle`, `make event-alpha-cycle-llm`, and `make event-alpha-cycle-send` now compose discovery/anomaly inputs, optional LLM metadata, alert ranking, watchlist refresh, and local router summaries into one research-only operating loop. The send path now uses only router-approved alertable decisions; broad event-alert digest sends remain limited to `--event-alert-report --event-alert-send`. |
| done | Polish Event Alpha operational semantics | — | Extractor `shadow` mode is analysis-only, `advisory` mode is the only extraction mode that can enrich raw evidence before deterministic discovery, effective playbooks now drive watchlist/router/snapshot grouping while rule playbooks are preserved for audit, route-approved Telegram copy has a dedicated renderer, alert copy uses playbook-specific hypothesis/timing/invalidation/verification, and alert snapshots/outcomes carry expected direction, primary horizon, success metric, primary-horizon return, direction-hit, and MFE/MAE cohorts. |
| done | Make Event Alpha tiering playbook-first | — | `event_alerts.resolve_playbook_alert_tier` now makes deterministic playbook assessment the primary research-tier input while preserving hard rejections and the invariant that only `proxy_fade` plus an `event_fade.py` `SHORT_TRIGGERED` signal can become `TRIGGERED_FADE`. Cluster confidence now informs scoring for accepted assets only, snapshot retention is configurable with `RSI_EVENT_ALPHA_SNAPSHOT_POLICY`, and `event_catalyst_search.py` provides offline query/evidence attachment scaffolding for market anomalies. |
| done | Add dynamic anomaly catalyst-search loop | — | `score_raw_event_for_llm_extraction` now prioritizes raw LLM extraction budgets by anomaly severity, freshness, catalyst keywords, asset mention quality, and noise penalties. `event_catalyst_search.py` now has provider/result/query models plus a fixture provider, bounded anomaly/query/result limits, and `main.py --event-catalyst-search-report`. `event_alpha_pipeline.run_event_alpha_operating_cycle` can now compose market anomalies → fixture catalyst search → attached raw evidence → optional LLM extraction → deterministic discovery/playbooks. Event graph links now preserve accepted kinds (`proxy`, `direct`, `supply`, `derivatives`, `infrastructure`), but cluster confidence only boosts proxy/direct/supply/derivatives rows. Playbook precedence now keeps explicit external proxy events out of loose listing playbooks unless the event type is truly an exchange/perp listing. |
| done | Operationalize dynamic catalyst search profiles | — | Event Alpha catalyst search can now use scored fixture/GDELT/RSS/CryptoPanic/Polymarket evidence adapters, reject low-quality results, report anomaly lifecycle states, and run named profiles (`fixture`, `no_key_live`, `no_key_llm`, `api_live`, `full_llm_live`, `research_send`) via `--event-alpha-profile` or `make event-alpha-cycle-profile`. LLM calls now have per-run/day/cache-TTL budget controls; exhausted budgets skip lower-priority rows fail-soft. All behavior remains research-only and still routes through deterministic discovery/playbooks before any alert tier. |
| done | Harden Event Alpha identity, budgets, and route lanes | — | Catalyst search now requires explicit anomaly-asset identity evidence before attaching catalyst hits, rejects common-word symbol false positives, records provider fetch/cache stats, and preserves identity reason codes. LLM extractor/relationship calls now share a persistent daily budget ledger under the research cache. Event alerts expose cluster confidence/source/time/link-kind components, watchlist rows mark material duplicate-state changes, router decisions include daily/instant/triggered lanes, and alert outcomes include playbook-specific volatility/up-then-fade/MFE-MAE metrics. |
| done | Add Event Alpha operational radar reports | — | Event Alpha cycles now append a JSONL run ledger, profile-aware status reports apply profile overrides before showing sources/LLM/budget/artifact paths, active watchlist rows can be monitored without new source evidence, missed-opportunity and calibration reports summarize large movers/feedback/outcomes, and Markdown research cards give per-alert review context. Catalyst-search identity now rejects URL-only and publisher/source-origin matches with explicit reason codes while allowing title/body/event text, contract path addresses, and validated LLM extraction evidence. All paths remain local research artifacts. |
| done | Polish Event Alpha daily operations | — | Profile-aware status now loads the default RSS URL list for all live RSS profiles, the unified Event Alpha cycle can opt into active-watchlist monitor routing via existing router lanes, run-ledger rows record send requested/attempted/success/delivery/block details, missed-opportunity diagnostics reject URL-only and publisher/source metadata identity hints, research cards can include Event Graph cluster context, and runbook targets cover daily report, daily LLM report, guarded send, health, and open-items loops. |
| done | Add Event Alpha self-improvement operations loop | — | Added a shared identity matcher used by catalyst search and missed-opportunity diagnostics, source-aware active-watchlist market refresh selection, source reliability reports, reviewable calibration-prior export, proposed eval-case exports from feedback/missed rows, research-card file output with `index.md`, last-run explain diagnostics, and `research/EVENT_ALPHA_RUNBOOK.md`. These paths remain research artifacts only and cannot apply priors, mutate canonical fixtures automatically, create `TRIGGERED_FADE`, trade, paper trade, or write normal RSI signal rows. |
| done | Polish Event Alpha daily-use operations | — | Active watchlist monitoring now has targeted market-provider selection and cache metadata, catalyst-search providers can be protected by local health/backoff state, calibration priors are reviewable through bounded shadow comparisons only, daily briefs can be written from run/watchlist/router/card/missed/calibration artifacts, quick feedback shorthands record manual labels, local replay compares stored artifacts without provider calls, and retention pruning is dry-run unless confirmed. The older opt-in prior-application path is superseded and runtime ranking remains unchanged. All paths remain research-only and cannot create `TRIGGERED_FADE`, normal RSI alerts, paper trades, live signal rows, or execution. |
| done | Polish Event Alpha policy-comparable daily radar | — | Provider health now covers live event sources plus universe/derivatives enrichment, discovery provider warnings flow into Event Alpha cycle warnings/run-ledger rows, targeted CoinGecko watchlist market lookup can refresh active assets by `coin_id` with TTL cache/fail-soft fallback, local raw-event replay can compare baseline/LLM/prior/router outputs without live providers or sends, priors shadow reports before/after tier and score effects without writing snapshots, daily briefs include provider health/LLM budget/new-vs-last/hotter watchlist/alertable/missed/calibration/suppression/why-alerts sections, research cards include a playbook-specific trade-readiness checklist, and burn-in/weekly Make workflows coordinate the daily research loop. |
| done | Build Event Alpha Radar design phases | — | Phase 1 raw-event extraction including upstream resolver hints, Phase 2 market enrichment/anomaly discovery, Phase 3 event-graph catalyst clustering plus persistent watchlist state, Phase 4 expanded deterministic playbooks, Phase 5 alert snapshot/outcome artifacts, artifact-only router decisions, lightweight feedback/evals, and the unified cycle command are now in place. `main.py --event-alpha-radar-report`, `--event-alpha-cycle`, `--event-watchlist-refresh`, `--event-watchlist-report`, `--event-alpha-router-report`, `--event-alpha-alerts-report`, `--event-alpha-fill-outcomes`, `--event-feedback-mark`, and `--event-feedback-report` cover the local research loop; `make event-alpha-eval`, `make event-alpha-alerts-report`, and `make event-alpha-fill-outcomes` cover the fixture/artifact paths. Details are in `DEVLOG.md`. Keep the system research-only until reviewed samples justify any separate promotion. |

## Later

No ready later items. Current remaining work is waiting on live outcomes, the
event-fade validation sample review status/labels/outcomes, Event Alpha
operational review data, or the next universe hygiene audit.
Tooling now exists for `--score --cohorts`, cost-aware backtests, walk-forward
checks, audit refreshes, event-fade fixture reports, event-discovery fixture/
universe plus opt-in live CoinGecko resolver enrichment/exchange/calendar/news
plus opt-in live Binance, Bybit, CryptoPanic, GDELT, RSS/RSS URL files, and Coinalyze
auto-symbol/external-catalyst/derivatives/supply radar
reports, research-only event-alert ranking reports, event-discovery JSONL cache refreshes, raw Binance
announcement cache listening, research-only LLM raw-extraction reports/evals,
research-only LLM shadow/advisory relationship reports/evals,
research-only event-alpha market-enriched/anomaly reports with playbook-first
tiering, catalyst-cluster confidence, identity-safe dynamic anomaly
catalyst-search reports/cycles with provider fetch/cache stats, watchlist state
refresh/report commands with material-change markers, active watchlist monitor
reports, last-run explain diagnostics, source reliability reports, calibration
prior exports, proposed eval-case exports, local router decisions with daily/instant/triggered lanes, alert
snapshot/outcome artifacts with
configurable retention policies and playbook-specific outcome metrics, feedback
artifacts, run-ledger/status/missed-opportunity/calibration/research-card
reports and card-file outputs, daily Markdown briefs, local artifact replay,
dry-run/confirmed artifact retention pruning, opt-in bounded calibration-prior
application, provider health/backoff state for event/catalyst/enrichment
providers, targeted active-watchlist CoinGecko refresh, priors shadow comparison,
raw-event local replay, daily burn-in/weekly review Make workflows, quick
feedback shorthands, and route/feedback evals, grouped
event-fade auto reports, event-fade validation-sample exports from fixtures or
cached snapshots, event-fade validation merge/price-export/outcome-fill/
labeling-queue/review-packet/review-template/review-bundle/review cohort
reports, and suspicious-kept audit leak detection.

## Maintenance Rules

- Update this file when a change completes, changes priority, gets blocked, or is
  intentionally dropped.
- Do not duplicate full history here. Put completed details in `DEVLOG.md`.
- Durable accepted/rejected technical choices belong in `DECISIONS.md`.
